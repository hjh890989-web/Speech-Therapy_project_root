# 대화기록 — 2026-05-26

> CLAUDE.md §6 일자별 단일 파일 규칙 정합 — 5/25 sub-session 3 후반부가 자정 넘어
> 5/26 으로 이어지면서 모두 `대화기록_2026-05-25.md` 에 누적됐었으나, 본 파일부터
> 분리. 본 일자의 신규 작업은 모두 여기 append.
>
> **누적 (5/26 단일 세션)**: 8 commits / vitest 2363 → 2369 (+6) / prod build 회복 ✅ /
> bundle 최대 -44.8% / 0 regression

## §1. 세션 흐름

5/25 sub-session 3 마무리 (commit `3c6bdac`) + migration prod 적용 (사용자 수동) 후,
5/26 자정 넘긴 시점에 이어진 추가 트랙.

본 일자의 작업은 4개 트랙으로 정리:
1. **Client bundle Prisma 누출 차단** (FR-PERF-3-CLIENT-LEAK-PARTIAL)
2. **Server Action use-server 룰 정합** (FR-PERF-3-USE-SERVER-REFACTOR — build 회복)
3. **CI 강화** (next-build job + TZ env)
4. **Performance 2차 trio 마무리** + **Performance 4차** (실측 dynamic import)

## §2. 트랙별 완료 작업

### 2.1 Client bundle Prisma 누출 차단

- `1dcf8d5` `lib/notifications/preference-shape.ts` (NEW) + `lib/offline-entry/types.ts` (NEW)
  - 5/25 §16 의 bundle-analyze 시도가 노출한 chain reaction 의 부분 fix
  - Client Component 가 상수/타입만 import 해도 ESM 그래프상 prisma 까지 transitively
    끌려가 Turbopack chunking failure (`node:module` 외부 모듈 미지원) 발생
  - 순수 shape (types/상수/normalize/shouldSend) 분리 → Client 측 안전 import
  - 기존 server-only callers (lib/email/*, RSC pages, tests) 는 re-export 로 변경 0

### 2.2 Server Action use-server 룰 정합 (build 회복)

- `3b504ea` Server Action 21개 일괄 shape 분리 (isolated worktree agent 위임)
  - Next.js 16 + Turbopack 의 strict 룰: `"use server"` 파일은 async function 외
    export 금지 (const/type/interface/class 불허). 21건 위반.
  - 각 Server Action 의 const/type/interface/class export 를 sibling
    `<name>-shape.ts` 로 분리 → 22 신규 파일 + Client Component 5개 import path 갱신
  - 추가 scope: `lib/analytics/funnel.ts` → `funnel-shape.ts` (FunnelChartImpl chunking
    에러 해소)
  - 추가 scope: `weekly-review/page.tsx` `chartWeeks` literal `viewedAt` 누락 보정
    (`0121bc3` 도입 후 미동기화된 pre-existing 회귀)
  - 결정: `"use server"` 파일에서 type-only re-export 시도 X — 모든 caller 가
    `-shape.ts` 직접 import (안전한 first-try, 회귀 0)

### 2.3 CI 강화

- `c8c3532` `.github/workflows/ci.yml` `next-build` job 추가
  - vitest + tsc + lint 모두 통과해도 `next build` 가 깨질 수 있는 사고 유형
    사전 차단:
    - `"use server"` 파일의 non-async export
    - Client Component 가 Prisma transitively import
  - 기존 "next build 의도적 제외" 정책 폐기 — Vercel CI 만 신뢰하면 main 에
    broken commit 누적 위험. 비용 trade-off: ~2~3분 << 사고 1회 1~2시간.
  - dummy env (DATABASE_URL / SUPABASE / GEMINI / BASE_URL) — compile + chunking
    만 검증, 실 DB/API 호출 0건.
- `646d1c1` CI `TZ=Asia/Seoul` env 추가
  - timeline/weekly-report/funnel 등 다수 테스트가 local Date 생성자
    (`new Date(2026, 4, 23, 14, 30)`) 와 `kstStartOfDay` (KST 자정) 비교
  - ubuntu-latest 기본 TZ=UTC 라 9h offset 으로 today/yesterday 분기가 어긋나
    vitest CI 만성적 빨강 (timeline-aggregator 2건 회귀)
  - quality-gate / next-build 두 job 모두 env 에 TZ 고정 → local 개발과 동일

### 2.4 Performance 2차 trio 마무리 + 4차 실측

- `d3145b2` weekly-reports cron → revalidateTag wiring (Performance 2차 trio 완결)
  - 새 WeeklyReport upsert 후 user 의 institutionId 기준
    `institution:<id>:dashboard` tag revalidateTag → 60s TTL 대기 X
  - prisma.user.findUnique select 에 institutionId 추가 (parentEmail 과 동시)
  - 루프 중 institutionId Set 누적 (같은 기관 N user → 1 invalidate)
  - 루프 후 institution 별 revalidateTag (Next.js 16 2-arg signature, `'default'`)
  - 모든 단계 graceful — findUnique/revalidateTag throw 시 console.error + cron 정상
  - +6 신규 테스트 (dedup / null / throw graceful)
- `ce752e4` **Performance 4차** — bundle-analyzer 실측 + 4 outlier dynamic import
  - `npm run analyze` (Turbopack `--experimental-analyze`) 첫 실측
  - `.next/diagnostics/route-bundle-stats.json` 분석 → 4 outlier 명확:

    | Route                                | BEFORE  | AFTER   | 절감     |
    |--------------------------------------|---------|---------|----------|
    | /admin/centers/pdf/[userId]          | 973 KB  | 537 KB  | -44.8%   |
    | /admin/students/import               | 825 KB  | 545 KB  | -33.9%   |
    | /login                               | 762 KB  | 528 KB  | -30.7%   |
    | /auth/reset-password                 | 757 KB  | 523 KB  | -30.9%   |

  - 전체 평균: 566 → 533 KB (-6%). max 973 → 550 KB (-43.5%)
  - 모든 라우트가 519~550 KB 범위 균질화 — baseline shared chunks 만 남음
  - 패턴: `type` import 만 정적 (compile time erased) + 실행 시점 (button click/
    file upload/form submit) 에 `await import(...)` lazy load. 회귀 위험 0

## §3. GitHub Actions Outage (5/26 11:00 UTC ~)

본 sub-session 후반부에 GitHub Actions 측 **critical outage** 발생:

- 시작: 2026-05-26 10:57:13 UTC
- 영향: 새 push 시 CI workflow 트리거 안 됨 / `workflow_dispatch` HTTP 500
- 본 sub-session 의 `c8c3532` / `646d1c1` / `d3145b2` 모두 outage 중 push 되어 CI run 미생성
- 복구 직후 (`ce752e4` push) CI 트리거됐으나 token 시스템 불안정으로
  Checkout 403 — 다음 push 시 자연 회복 예상

**중요**: 본 사실은 코드/CI 설정 무관 — Vercel deploy 는 별도 (자체 build, GitHub
Actions 무관) 으로 안전.

## §4. pre-existing CI 버그 fix (본 PR)

- prisma-drift job 의 Guard step 이 `defaults.run.working-directory: Speech-Therapy_App`
  적용으로 Checkout 전 디렉토리 부재 → bash 시작 실패
- Guard step 에 `working-directory: .` override 추가 (root 에서 단순 secret 검사만)

## §5. 신규 후속 작업 (남은 잔여)

5/25 §18 항목 중 미진행 + 본 일자 발견:

- **Bundle analyze 후속 (Performance 5차)** — baseline 519 KB 의 shared chunks 분석:
  React/Supabase/Prisma 의 어느 부분이 baseline 차지하는지 → 추가 tree-shake 가능?
- **consent reminder 페이지 UI** — `dca2aee` badge 클릭 시 ConsentSignature 발송/재발송 화면
- **2FA recovery flow** — admin 측 backup codes 소진 시 unenroll path
- **parent invite 이메일 React Email 템플릿** — 현 text → 디자인 템플릿 (4종 통일)
- **Student 전용 Prisma 모델 refactor** — `User.role='parent'` → ChildProfile 분리 (大)
- **prisma-drift Guard step bug** — ✅ 본 PR 에서 fix
- **CI 회복 후 next-build job 효과 검증** — 다음 push 시 자동 확인

## §6. 운영 적용 안내

본 일자 신규 migration 0건 (모든 migration 은 5/25 분). prod 적용 불필요.

Vercel 환경변수 추가도 없음 — 5/25 §15 / §10 그대로 유효.

다음 Vercel deploy 시 build 정상 통과 (로컬 + CI 양쪽 검증됨).

---

## §7. sub-session 2 — Performance 5차 + 3-way 병렬 (consent/2FA/email)

> §1~§6 마무리 (`327248d`) 후 이어진 4 commits.
> **누적 (5/26 최종)**: 11 commits / vitest 1722 → 2440 (+718 / 0 regression) /
> prod build 회복 / bundle 최대 -44.8% / 3-way 병렬 트랙 완결

### 7.1 Performance 5차 — baseline 519 KB 분석 (no-go)

`ce752e4` Performance 4차 직후 자연 후속으로 baseline shared chunks (전 35 route
공통 519 KB) 분석 시도.

| Chunk | 크기 | 식별 |
|---|---|---|
| `0pvbivsa2scw9.js` | 222.2 KB | React + scheduler + Next.js router (framework core) |
| `0elroro53r.mx.js` | 107.2 KB | Next.js App Router internals |
| `0aq~4wa-gdmym.js` | 53.4 KB | Next.js shared utilities |
| 나머지 6 chunks | 136 KB | Turbopack runtime + 핵심 shared app code |
| **합계** | **519 KB** | — |

**결론: no-go (diminishing returns)** — baseline 은 Next.js 16 + React 19 +
Turbopack 의 불가피한 framework floor. Root layout 추가 import (`@vercel/analytics`,
`@vercel/speed-insights`, `OfflineToast`, `ServiceWorkerRegister`) 도 모두 정당한
critical UI. 추가 최적화는 framework 자체 upgrade 외엔 없음.

**의미**: Performance 트랙 (1차 → 2차 trio → 3차 client leak / use-server →
4차 outlier dynamic import) 완결. 다음 Performance 작업은 운영 데이터 (Vercel
Speed Insights) 기반 LCP/CLS hot path 발굴 (별도 sub-session).

### 7.2 GitHub Actions outage 부분 복구

§3 의 outage 가 본 sub-session 중 부분 복구:
- workflow trigger 정상화 (push 시 CI run 자동 생성)
- 단 첫 run 의 `actions/checkout@v4` 가 token 시스템 불안정으로 HTTP 403 — 다음
  push 시 token 안정화 예상
- 본 sub-session 의 추가 push 시 token 회복 자연스러움 검증 예정

### 7.3 prisma-drift Guard step bug fix

- `327248d` 에 포함 — pre-existing CI 버그. Guard step 이 `defaults.run.working-
  directory: Speech-Therapy_App` 적용으로 Checkout 전 디렉토리 부재 → bash 시작
  실패. Guard step 만 `working-directory: .` override 로 해소.
- 효과 확인: 본 sub-session 의 CI run 에서 prisma-drift job success (secrets
  미설정 graceful skip) — quality-gate / next-build 는 여전히 Checkout 403 (위
  outage 잔여).

### 7.4 3-way 병렬 트랙 — consent UI / 2FA recovery / React Email pilot

isolated worktree agent 3개 동시 spawn — 5/25 의 3-way (consent badge / cursor /
tz) 와 동일 패턴.

#### `88b73c1` B1 — FR-CONSENT-REMINDER-UI (parent self-service)

`dca2aee` (5/25) badge UX 완성. parent 가 badge 클릭 시 본인 pending
ConsentSignature 목록 확인 + 재발송 트리거.

- `app/(public)/settings/consent/page.tsx` (NEW) — RSC, 본인 email 의 pending
  ConsentSignature 목록 + 빈 상태
- `app/actions/resend-consent-reminder.ts` + `-shape.ts` (NEW) — `"use server"` rule
  정합. 본인 parentEmail 만 (R4 cross-user 차단)
- `components/settings/ConsentResendButton.tsx` (NEW) — state machine
  (idle/sending/success/error)
- `app/(public)/settings/page.tsx` 에 "동의서 관리" 카드 추가
- markReminded on opt-out — 자동 cron 과 동일 정책 (spam 방지)
- 신규 테스트 27건

**비자명 결정**: MainNav badge target 은 `/settings` 유지 (applyBadgeCounts pure
decorator, role 분기 위험) — bridge 는 settings index 의 카드.

#### `6c865fc` B2 — FR-2FA-RECOVERY (admin TOTP reset)

backup codes 8개 모두 소진 + authenticator 분실 → 영구 lockout 회피 path.

- `app/admin/security/totp-reset/page.tsx` (NEW) — admin-only RSC + L2 RBAC
- `app/actions/admin-reset-totp.ts` + `-shape.ts` (NEW) — Supabase Admin SDK
  `auth.admin.mfa.{listFactors, deleteFactor}` + `totpBackupCodes = []` clear
- `components/security/AdminTotpResetForm.tsx` (NEW) — email 확인 입력 (delete-
  account 패턴) + state machine
- `lib/audit/critical-alert.ts` 의 `totp_disabled` 트리거 (Slack alert)
- `withActor(callerId, ...)` — admin이 actor (AuditLog 정합)
- `components/nav/MainNav.tsx` admin role 에 "🔐 2FA 초기화" 메뉴 entry 추가
- 신규 테스트 33건

**비자명 결정**:
- Dual auth check (Supabase getUser + Prisma role cross-check) — JWT cache 의
  role change race 보호
- Admin SDK 부재 시 DB-side clear 만 진행 (dev/preview safety) — factorsUnenrolled=0
- `withActor(callerId)` (NOT targetUserId) — admin 이 action 수행 actor

#### `5514559` B3 — FR-EMAIL-REACT-TEMPLATE (parent-invite pilot)

string-concat HTML → `@react-email/components` 컴포넌트. 4 이메일 중 parent-invite
만 pilot.

- `lib/email/templates/ParentInviteEmail.tsx` (NEW) — React Email primitives
  (Html / Body / Container / Heading / Text / Button / Section / Hr / Link / Preview)
- `lib/email/parent-invite-email.tsx` (NEW) — `renderParentInviteEmail` async
  function (`@react-email/render` 위임)
- `lib/email/templates.ts` `buildParentInviteEmail` async 변경 — `parent-invite-
  email.tsx` 동적 import 위임 (JSX-in-`.ts` 회피 + server-only barrier)
- `app/actions/parent-invite.ts` — `await buildParentInviteEmail(...)` + render
  실패 → `email_failed` graceful
- `package.json` — `@react-email/components ^1.0.12` + `@react-email/render ^2.0.8`
- 신규 테스트 11건

**비자명 결정**:
- React Email 은 server-only — client bundle 영향 0 (`.next/static` 에 deps 0건)
- 첫 cold render 5s+ → 테스트 timeout 30s bump
- 다른 3 emails (cushion-note / weekly-report / consent-reminder) 는 별도 PR

### 7.5 lib/events.ts 3-way 충돌 해결

세 agent 모두 union 끝에 신규 event 추가 → 2회 conflict (B2 cherry-pick + B3
cherry-pick) 모두 trivial — 3 events 보존 (consent_reminder_resent /
admin_totp_reset / parent_invite_email_rendered).

5/25 의 3-way (consent badge / cursor / tz) 와 동일 패턴 — agent 에게 "union 끝
에 추가" 룰 명시한 결과.

### 7.6 검증

- vitest **2440 / 2440 passing** ✅ (1722 → 2440, +718 / 회귀 0)
- `next build` 통과 ✅
- 4 worktree 모두 cleanup
- 11 commits 모두 push (origin/main = `5514559`)

## §8. 신규 후속 작업 (남은 잔여, sub-session 2 추가)

§5 항목 + 본 sub-session 의 신규:

- **B3 후속: 나머지 3 emails React Email migration** (cushion-note / weekly-report
  / consent-reminder) — pilot 패턴 검증됨, 1~2 commit 규모
- **Performance 5차 alternative — Vercel Speed Insights 측정 기반 LCP/CLS hot
  path 발굴** (운영 데이터 의존, 새 sub-session)
- **Student 전용 Prisma 모델 refactor** — 大, 새 sub-session 권장
- **CI 회복 검증** — outage 잔여로 quality-gate / next-build 여전히 Checkout 403,
  다음 push 시 token 안정화 자연 회복 예상

## §9. 운영 적용 안내 (sub-session 2)

본 일자 신규 migration 0건 유지. prod 적용 불필요.

Vercel 환경변수도 추가 없음 (B2 는 기존 `SUPABASE_SERVICE_ROLE_KEY` 재활용, B3 는
`RESEND_API_KEY` 재활용).

다음 Vercel deploy 시 build 정상 통과 — local + B3 의 React Email 도 server-only
boundary 검증됨.

---

## §10. Vercel deploy 차단 진단 (5/26 → 5/27 새벽, 미해결)

> §7~§9 마무리 (`d18a1d7`) 후, 사용자가 prod 사이트 확인 시 **새 commits 미반영**
> 발견. 진단 + 부분 fix 시도 1건 (`c650c31` empty commit) — Vercel 측 issue 로
> 미해결 상태. 다음 sub-session 으로 인계.

### 10.1 발견 — Prod 가 4일 이상 stale

사용자가 https://speech-therapy-project-root.vercel.app/ 접속 시 정상 응답되나:
- HTTP response 의 `Age` 헤더 ~4.3일 (5/22 즈음)
- `/settings/consent` (88b73c1, 5/26) → 404
- `/admin/security/totp-reset` (6c865fc, 5/26) → 404

→ Vercel 의 마지막 deploy 가 5/22 (`78bc42b`). 그 후 push 한 ~30+ commits 모두
prod 미반영.

### 10.2 진단 단계별 시도

| # | 시도 | 결과 |
|---|------|------|
| 1 | Vercel UI "Redeploy" | 단지 5/22 build 의 재배포 (코드 변경 X) — 의미 0 |
| 2 | Settings → Git 의 Disconnect → Reconnect (Connected May 13 → just now) | 재연결 성공, but 자동 trigger 안 됨 |
| 3 | Empty commit push (`c650c31`) — webhook 강제 발화 시도 | Deployments 탭에 새 entry 0건 |
| 4 | Settings → Git → Deploy Hooks 생성 + `curl -X POST` (Claude 환경 3회) | `{"job":{"state":"PENDING"}}` 응답 3회, 단 entry 생성 0 |
| 5 | Settings → Environments — Production Branch = `main` 확인 | 정상 |
| 6 | Settings → Git 의 Production Branch / Ignored Build Step | 섹션 부재 (Vercel UI 최신 layout) |
| 7 | Vercel Usage / Billing 확인 | 모든 메트릭 0~3% (한도 충분) — 자원 한도 폐기 |
| 8 | Status filter 6/6 (Canceled 포함) | 그래도 새 entries 0건 — Canceled 도 아님 |
| 9 | `npx vercel --prod` (CLI 직접 deploy) | `Error: connect ETIMEDOUT 76.76.21.112:443` |

### 10.3 ETIMEDOUT — 사용자 PC 의 network 차단

CLI 의 `76.76.21.112` = api.vercel.com.
- ✅ Dashboard (vercel.com web UI) 정상 접속
- ❌ api.vercel.com 만 timeout

가능한 원인:
- Windows Firewall / 백신 (V3 / Norton 등) 의 anycast IP 차단
- ISP (KT/SKB) 라우팅 일시 장애
- DNS resolve 실패 (제대로 된 IP 가 아닐 가능성)
- 회사/학교 네트워크 방화벽

**해결책 후보**: DNS 변경 (1.1.1.1 / 8.8.8.8) / 모바일 핫스팟 전환 / 방화벽 일시
비활성화. — 다음 sub-session 에서 시도 권장.

### 10.4 Claude 환경의 Deploy Hook fire 도 entry 생성 0건

Claude Bash 환경에서 3회 Deploy Hook 호출 모두 PENDING 응답 받았으나 Vercel
Deployments 탭에 새 entry 0건. 이건 user network 무관 (Claude 환경에서 API 접근 OK).

→ **Vercel platform 측 issue 추정**:
1. Vercel 의 GitHub source pull 일시 장애 (GitHub Actions outage 잔여 영향?)
2. Hobby project 의 build queue 내부 문제
3. Deploy Hook job 처리가 silent 하게 실패

### 10.5 잠정 결론

- Vercel 의 새 deploy trigger path 가 어떤 방식으로 호출돼도 (push webhook /
  Deploy Hook / CLI) 새 deployment entry 자체가 생성 안 됨
- 단 redeploy (기존 build 재배포) 는 정상 — Vercel 의 storage / serving 은 OK
- 추정: Vercel internal queue 또는 GitHub integration 의 source fetch 일시 장애

**현재 prod 상태**:
- 5/22 build (`78bc42b`) 가 그대로 서빙 중
- 5/22 ~ 5/27 동안의 모든 작업 (Performance 1~4차, 3-way 병렬, audit, consent UI, 2FA
  recovery, React Email pilot 등) **prod 미반영**
- 단 코드/build/test 자체는 모두 안전 (local + CI 검증)

### 10.6 다음 sub-session 진입 시 권장 액션

| 우선순위 | 액션 | 비고 |
|---|---|---|
| 1 | **1~2시간 후 단순 `npx vercel --prod` 재시도** | Vercel 측 자동 회복 시 즉시 해결 |
| 2 | **모바일 핫스팟 전환** 후 CLI 재시도 | user network 우회 |
| 3 | **Vercel Status 페이지 확인** (https://www.vercel-status.com/) | incident 진행 여부 |
| 4 | 위 모두 실패 시 **Vercel Support 문의** | Hobby tier 도 가능 |
| 5 | 최후 — **Vercel project 재생성** | URL 변경 위험 (`*.vercel.app` 자동 도메인) |

### 10.7 보안 정리 잔여

본 sub-session 에서 Deploy Hook URL 채팅/스크린샷에 노출됨 (대화기록 git commit
포함). 다음 sub-session 진입 시:
- Settings → Git → **Deploy Hooks** 의 `manual-trigger` hook 삭제
- 필요 시 새로 생성

### 10.8 push 한 commits 정리 (5/27 새벽)

- `c650c31` chore: trigger Vercel deploy after Git integration reconnect (empty
  commit) — Vercel webhook 강제 발화 시도용. 미작동.

### 10.9 인계 메모

본 issue 는 **코드/build 측 문제 0건**. Vercel platform / network 측 issue 만으로
prod 가 stale. 다음 sub-session 진입 시:
- 위 §10.6 의 액션 1 (`npx vercel --prod` 단순 재시도) 부터 시도
- 1번이 작동하면 5/22 ~ 5/27 의 모든 작업 (~30+ commits) prod 일괄 반영됨
- 작동하지 않으면 §10.6 액션 2~4 순차 진행


# 대화기록 — 2026-06-02

> 저장 위치: `Prompt/대화기록_2026-06-02.md`
> 직전 기록: `대화기록_2026-06-01.md` §0~8 (06-01 — `/goal` 테스트 백필 루프 QUEUE_EMPTY + cron 2줄 + SystemConfig 배포 + funnel-realdata 정직 검증·교차 계약 가드, vitest 2967→3004).
> 본 문서 핵심: **F15(/chat 이야기 친구) Production 활성** — TEST-020 swap-terminal 정본 → e2e CI job → (발견) CI red 복구 → Preview 검증 → **Production flip + prod 실검증 완료**.

---

## 0. 세션 작업 요약 (2026-06-02)

| 항목 | 값 |
|---|---|
| **신규 commits** | 3 (`3402f5b`·`92fc0d6`·`07bcc0e`), 전부 origin/main push |
| **운영(owner Vercel)** | **F15 Production 활성** (`F15_CHAT_ENABLED=true`, Preview→Prod 단계 flip) |
| **vitest** | 3003 → **3004 PASS** (TEST-020 swap-terminal lock +1, 회귀 0) |
| **CI** | 05-31부터 red(eslint)였음을 발견 → **react-hooks lint 3건 복구로 전체 green** + e2e-chat job 신규 |
| **throwaway** | PR #107 + `preview/f15-smoke` 브랜치 (Preview 트리거용, 검증 후 정리) |
| **성격** | F15 활성 end-to-end (BLOCKING 해소 → e2e CI → CI green → Preview→Prod 활성·실검증) |

---

## 1. TEST-020 swap-terminal 정본 확정 — `3402f5b` (F15 활성 BLOCKING 해소)

- 6/2 시작 시 어제 기록 확인 → 무게이트 backlog 소진, 남은 건 결정/게이트 선행. 사용자가 **F15 활성 점검** 선택.
- F15 점검(3 facet 병렬 조사): **하드 게이트 전부 해소 확인** — 임상 13항목 통과(05-31 박제) / IRB 조건부 미해당 / PIPA 동의 fail-closed / ChatMessage 격리 / 7일+계정삭제 폐기 / CON-04 3-layer / 동의 UI 존재. 단일 잔여 = `F15_CHAT_ENABLED` + **TEST-020 spec↔code 충돌(BLOCKING)**.
- **충돌**: spec(TASK_TEST-020)은 "금칙어 시 재생성 1회", 코드([filterStream](../Speech-Therapy_App/lib/ai/profanity-filter.ts))는 **즉시 안전폴백+종료(retry 없음)**. 사용자 결정 = **swap-terminal 정본 채택**(안전·저비용·결정적, 3-layer 일관).
- 처리: profanity-filter.test 에 **swap-terminal 결정 잠금 테스트**(후속 clean 청크 미방출 + 폴백 1회 = retry 부재 가드) + [TASK_TEST-020](../Speech-Therapy_App/tasks/TASK_TEST-020.md) 4곳 정합(proxy.ts→Route Handler stream transform, 폴백 멘트 실제값). 소스 변경 0. vitest 3004.

## 2. F15 /chat e2e CI job 추가 + BANNED 오탐 수정 — `92fc0d6`

- 진단: [ci.yml](../.github/workflows/ci.yml)이 playwright e2e 를 **의도적 제외** → "별도 워크플로"가 부재. "webServer 부팅 이슈"는 로컬 E: drive symlink 한정(CI ubuntu 무관).
- 로컬 de-risk: `next start`(prod) + `PLAYWRIGHT_BASE_URL` + `F15_CHAT_ENABLED=true` 로 chat-flow 실행 → **e2e 최초 실행으로 진짜 버그 노출**.
- **버그 fix**: chat-flow BANNED 에서 `의료적 판단` 제거 — 전역 `MedicalDisclaimerFooter`(REQ-NF-028)의 "의료적 판단이 필요한 경우 의료기관 진료" 는 비의료기기 고지의 **합법·필수 문구**이며 CON-04 위반 아님(canonical=치료/진단/장애). 그간 e2e 가 `--list` 로만 수집돼 잠복.
- ci.yml **`e2e-chat` job 신규**: next build + start 수동 기동 + PLAYWRIGHT_BASE_URL(webServer skip) + `F15_CHAT_ENABLED=true` → ON describe 실행. 로컬 ON 10 passed.

## 3. ⚠️ pre-existing CI red 발견 + react-hooks lint 복구 — `07bcc0e` (CI 전체 green)

- 푸시 후 `gh run`으로 확인하니 **CI가 직전 여러 커밋부터 red** — 원인 = quality-gate **eslint 에러 3건**(`tsc→eslint→vitest` 순차라 vitest 도 CI 미실행). **제 최근 커밋이 유발한 게 아니라 05-31 STT/voice 작업 이후 누적**(직접 main push 패턴이라 미발견).
- 수정(동작 보존): [useSpeechToText](../Speech-Therapy_App/lib/hooks/useSpeechToText.ts) onResultRef 갱신 render→useEffect(react-hooks/refs) / [useVoiceActivity](../Speech-Therapy_App/lib/audio/useVoiceActivity.ts) reset effect 자기 state dep 제거 + 의도된 reset-on-disable 국소 disable(set-state-in-effect) / useSpeechToText.test fake 인스턴스 캡처 no-this-alias 국소 disable.
- 검증: lint 0 errors / tsc 0 / vitest 3004 / build 0. push 후 **CI 4 job 전부 success**(quality-gate vitest 재가동 + e2e-chat green). ⇒ **회귀 안전망 복구**.
- 학습 박제(메모리): 푸시 전 게이트에 **`npm run lint` 포함** + 푸시 후 `gh run` 확인.

## 4. F15 Preview 검증 (단계적 활성 1단계)

- 사용자 결정 = **Preview 먼저**(가역적·안전). owner Vercel: `F15_CHAT_ENABLED=true` **scope Preview만** 추가. (`GOOGLE_…API_KEY` 이미 Production+Preview → preview 실 Gemini 가능.)
- 일반 브랜치 push 로는 preview 미생성(PR 워크플로 부재) → `preview/f15-smoke` 브랜치 + **빈 커밋 + draft PR #107**(DO NOT MERGE)로 Vercel Preview 트리거.
- 검증 결과: preview /chat **F15 ON UI** ✅ / 비로그인 401 매핑 ✅ / **prod /chat = "준비 중"(F15 OFF) → 게이트 scope 정확·prod 안전** ✅. 
- ⚠️ **학습(메모리)**: preview 는 **Vercel SSO 보호**(자동 e2e 401 차단) + **로그인이 prod 도메인으로 redirect**(`NEXT_PUBLIC_SITE_URL`) → preview 에서 "로그인+실 채팅"은 검증 불가. ⇒ 로그인+실데이터 경로는 prod flip(가역) 후 검증으로 결정.

## 5. ⭐ F15 Production flip + 실검증 (활성 완료) — owner Vercel

- owner: `F15_CHAT_ENABLED` scope에 **Production 추가**(→ Production and Preview) + **Production redeploy**.
- (혼선 1건) 직후 브라우저에 "준비 중" → **브라우저 캐시**였음. fresh curl 로 prod `/chat` = **채팅 UI(HTTP 200) 서빙 확인** → 활성 성공 입증.
- **prod 실검증**(시크릿창/로그인+이중동의 완료 상태): 채팅 UI ✅ / 로그인 후 **nav "이야기 친구" 링크** ✅ / STT 마이크 ✅ / 비로그인 401 ✅ / **"병원 가야 해"(금칙어) → 안전폴백 "우리 같이 천천히 한 번 더…"** ✅✅ — **TEST-020 swap-terminal 이 prod 실환경에서 그대로 작동**.
- ⇒ **F15 (/chat) Production 활성 완료.** 롤백 = `F15_CHAT_ENABLED` Production 체크 해제 + 재배포(즉시 dormant).
- 정리: PR #107 close(머지 없음) + `preview/f15-smoke` 브랜치(원격·로컬) 삭제.

## 6. 세션 메타

- **F15 출시**: 오늘 BLOCKING(TEST-020)부터 e2e CI·CI green 복구·Preview→Prod 활성·실검증까지 한 호흡에 마감. 자녀 대화 **안전 폴백(swap-terminal)** 이 prod 에서 확인됨이 핵심 성과.
- ⚠️ **CI red 누적 교훈**: 직접 main push 패턴 + 로컬 lint 미실행 → CI red(vitest 미실행)가 05-31부터 잠복. 푸시 전 lint + 푸시 후 `gh run` 습관화(메모리 박제).
- ⚠️ **Preview 검증 한계**: SSO 보호 + auth→prod redirect 로 로그인 경로는 prod 에서만. env flag 가역성 덕에 prod flip+검증이 안전(메모리 박제).
- ⚠️ **push 정책**: test/docs push 는 자동, **CI/production 자동화 변경 push 는 명시 승인**. 본 세션도 매 push 승인 확인.
- **잔여(F15 후속, 급하지 않음)**: 일반 발화 실 Gemini 창작응답 prod 확인(안전경로는 검증됨) / push-dispatch(F16) 활성(D5 부활) / CI Node20→24 actions deprecation(6/16) + unused-var warning 4건 정리.

---

**— End of 대화기록 2026-06-02 — F15(/chat) Production 활성: TEST-020 swap-terminal 정본 → e2e-chat CI job → react-hooks lint 복구(CI 전체 green) → Preview 검증 → Prod flip + 실검증(금칙어→안전폴백) (vitest 3004, 3 commits + Vercel 활성) —**

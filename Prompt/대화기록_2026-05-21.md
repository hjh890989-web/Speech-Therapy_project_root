# 대화기록 — 2026-05-21

> 저장 위치: `Prompt/대화기록_2026-05-21.md`
> 운영 규칙: 일자별 단일 파일. 2026-05-20 sub-session A (P1 6 tasks) 종결 후 본 파일에서 연속 진행.
> 본 session 핵심: **P0 100% 종결 + P1 7건 + 병렬 패턴 3회차 정착** — SEC-004 / FR-C-003 / FR-C-004 / DB-004 / SP3_2A-3 / MON-004 Status / OPS-001 / FR-Q-005 잔여 / FR-C-011 / TEST-014.

---

## 0. 세션 작업 요약

### 정량 결과

| 항목 | 값 |
|---|---|
| **commit** | 16 (`a44e774` ~ `0dca38f`) |
| **push** | 9 회 (사용자 승인 후) |
| **줄수 변경** | +4,400 -300 (approx) |
| **vitest** | 301 → **383 pass** (+82, 회귀 0) |
| **vitest 파일** | 33 → 38 (+5) |
| **lint / tsc** | 0 / 0 (매 commit) |
| **신규 lib API** | `predictNextScore`, `countDismissedThisMonth`, `countReviewsToday`, `assessDataSufficiency`, `computeWeekOverWeekDelta`, `shouldRecommendRest` 등 7+ |
| **신규 component** | `Status Page` (`/status`), 재설계된 `PredictionCard`, `SilenceInterventionBanner`, `RestEmptyState`, `ReportEmptyState` |
| **신규 docs** | `ops-runbook.md` (350줄), `cs-templates.md` (161줄), `postmortem-template.md` |
| **신규 events** | `prediction_calculated` / `prediction_simulation_changed` / `prediction_clicked` / `mission_*` 외 |
| **병렬 Agent 호출** | 3회차 (6 task) — 모두 회귀 0, 충돌 0 |

### 본 sub-session 위치

- 2026-05-20 sub-session A (P1 6 tasks) 종결 직후
- 본 PC 단일 세션 — 컨텍스트 압축 없이 8 task 연속 + 병렬 3회차
- 진입점: 5/20 §7.3 우선순위 표
- 다음 진입점: §9 우선순위 (Option B / P2 issues / 외부 GUI setup)

### Task → Commit 매핑

| sub | commit | task | type |
|---|---|---|---|
| B | `a44e774` | SEC-004 ratelimit 강화 (일 비용 + 80% Slack + envPrefix) | feat |
| C | `f70cad4` | FR-C-003 STT 재시도 trackEvent 통합 | feat |
| D | `1746199` | FR-C-004 음성 미저장 정책 docs + D6 schema vitest | docs |
| E (병렬 1) | `306912e` + `36ef2df` | DB-004 schema 검증 + SP3_2A-3 FFT 2x | test + feat |
| F (병렬 2) | `8415bf4` + `96203bd` | MON-004 Status Page + OPS-001 docs | feat + docs |
| G (병렬 3) | `ecf36df` + `8eddcb9` | README CON-04 + FR-Q-005 잔여 (WoW + prediction_clicked) | docs + feat |
| H | `a8cb2f5` | FR-C-011 Gemini 회귀 예측 + 시뮬레이션 | feat |
| I | `0dca38f` | TEST-014 HITL 9 시나리오 + abuse 방어 + expert overload | feat |

---

## 1. Sub-session B — SEC-004 (Gemini Rate Limiter 강화)

### 진입 + 발견

- 5/20 §29.3 의 🟢 P0 후보 — 사용자 우선순위 결정
- 기존 `lib/ratelimit.ts` 가 §E-1 (in-memory P0: 글로벌 RPM 14 + 사용자 일 50) 이미 완료
- 누락 = §E-2 항목: Slack 80% 알림 / 비용 추정 / 환경 prefix

### 구현 — `lib/ratelimit.ts`

- 일 글로벌 호출 카운터 + 비용 추정 ($0.000060/call, gemini-2.5-flash-lite)
- 80% 임계 도달 시 Slack 알림 1회 (fire-and-forget, 중복 방지)
- 자정 자동 리셋 (sliding 24h) — alert flag + 카운터 동시
- 환경 prefix (`VERCEL_ENV` → `NODE_ENV` → "dev") — Vercel ENV 별 격리
- `getRateLimitDailyStats()` 모니터링 export

### README 신규 섹션

"Gemini Rate Limiter (SEC-004)" — 한도/비용모델/env vars/통합 패턴/§E-2 후속.

### 결과

- AC 6/6 충족 (§E-2 Upstash Redis 는 AGENTS.md §3 정책 변경 별도)
- vitest +10 (총 311)
- 별도 task 명시: Upstash Redis 도입 시 본 모듈 어댑터로 교체

---

## 2. Sub-session C — FR-C-003 (STT 재시도)

### 발견

- `lib/hooks/useSpeechRecognition.ts` 의 재시도 1회 / 200ms 로직 + RETRYABLE codes 이미 구현됨
- 누락 = INFRA-005 연결 (CustomEvent dispatch → `trackEvent` 교체)
- `lib/events.ts` 카탈로그 stt 분기 부재

### 구현

- `events.ts`: stt_first_attempt_success / stt_retry_success (firstAttemptError) / stt_retry_failed (finalError) 추가
- useSpeechRecognition: CustomEvent dispatch → `trackEvent` 호출로 교체
- `firstAttemptErrorRef` 신규 — 재시도 성공 시 첫 에러 분류 노출
- `isRetryableTransient()` type guard

### 결과

- AC 3/4 즉시 + Scenario 3 (성공률 ≥ 98% 부하 시뮬) 은 별도 TEST-003 P1
- vitest +5 (총 316)

---

## 3. Sub-session D — FR-C-004 (음성 7일 폐기)

### 발견 — 코드 거의 완성

- `/api/cron/audio-cleanup/route.ts` 풀 구현 (DB cleanup + Storage purge + verifyCronSecret)
- vitest 7건 기존
- 누락 = 정책 문서화 + AC 1 (음성 미저장) 입증

### 구현

- README 신규 섹션 "음성 미저장 정책 (FR-C-004 / D6)":
  - 보장 메커니즘 5계층 (클라이언트 STT / Server Action input / DB / Storage / Cron)
  - REQ / R 매핑
  - P2 활성화 가이드 6단계
- vitest D6 schema 강제 2 케이스 — `DiagnosisInputSchema` 가 audio binary 필드 거부 + 의심 키워드 0건 sanity

### 결과

- AC 3/4 즉시 + Storage RLS 외부 GUI 별도
- vitest +2 (총 318)

---

## 4. Sub-session E — 병렬 1회차 (DB-004 + SP3_2A-3)

### 결정 — 첫 Agent 병렬 실험

- 사용자 권장: "병렬 진행 가능"
- 두 task 변경 파일 disjoint (prisma/schema vs audio/analyzer)
- 각 Agent isolated worktree (충돌 0)

### Agent A — #16 DB-004

- 발견: SessionLog 모델 + `audioVectorUri` nullable + cascade FK + `[userId, startTime]` 인덱스 모두 **이미 spec 100% 충족**
- 결과: 코드 변경 0, 회귀 방지 정적 검증 17 케이스만 추가 (`__tests__/security/schema-db-004.test.ts`)
- commit `306912e` (+118)

### Agent B — #103 SP3_2A-3 Option A

- FFT_SIZE 2048 → **4096** (bin 분해능 21.5 → 10.7 Hz, 2x 정밀)
- SP3_2A-2 파라미터 (50ms / -70dB / 0.003) 보존
- 트레이드오프: CPU ~2x, memory ~2x, AnalyserNode 상한 32768 여유
- 회귀 방지 vitest 1건 추가 (`vi.fn(function NamedCtor() {...})` 패턴)
- commit `36ef2df` (+81 -4)

### 통합 검증 (cherry-pick)

- main 위 2 commit ahead → fast-forward / cherry-pick
- 합산: lint 0 / tsc 0 / vitest **336 pass** (+18, 회귀 0)

### 학습

- **isolated worktree 가 충돌 0 보장** — 안전한 병렬 패턴
- 사용자 입력 1회 (검토/승인) → 2 task 동시 완료

---

## 5. Sub-session F — 병렬 2회차 (Status Page + OPS-001)

### Agent A — #68 OPS-001 (docs)

- `docs/ops-runbook.md` (350줄): CS 4h SLA / HITL 48h 흐름 / Supabase Studio 워크플로 / 인시던트 매트릭스
- `docs/cs-templates.md` (161줄): 6종 CS 카피 (CON-04 의료 금칙어 0건)
- README "참고 문서" +4 줄
- 코드 변경 0
- commit `96203bd` (+515)

### Agent B — #67 MON-004 Status Page

- `/status` RSC — `import { GET as getHealth }` 직접 호출 (HTTP 라운드트립 회피, Next.js 16 RSC 패턴)
- 색맹 대응 3중 (color + emoji + text)
- 모바일 first (44px+, grid 1→3열)
- 통합 테스트 7건 (healthy/degraded/unhealthy 3분기 + 503 fallback + 접근성)
- commit `8415bf4` (+520, page 309 + test 209 + README 2)

### 통합 검증

- README auto-merge OK (다른 섹션)
- 합산: vitest **343 pass** (+7, 회귀 0)

### 부수 발견

Agent A 가 보고: README 76/164 줄에 의료 금칙어 "진단" 2회 잔존 (본 작업 scope 외). → sub-session G 에서 정리.

---

## 6. Sub-session G — 병렬 3회차 (README CON-04 직접 + FR-Q-005 잔여 Agent)

### 새 패턴 — 직접 + Agent 병렬

- README "진단" 2회 정정은 trivial → 직접 처리 (즉시)
- FR-Q-005 잔여는 비교적 큰 작업 → Agent (background)
- 파일 disjoint: README vs `lib/weekly-report.ts` + `app/(public)/reports/`

### 본인 직접 — README CON-04

- L76 "5분 진단" → "5분 발음 확인" (PRD V10 카피)
- L166 "진단 결과 DB" → "평가 결과 DB"
- 검증: `grep -E "진단|치료|장애" README.md` → 0 hit
- commit `ecf36df` (+2 -2)

### Agent — FR-Q-005 잔여 (#46 Scenario 2 + 4)

- `computeWeekOverWeekDelta(current, previous)` 순수 함수 + `previousWeek(year, week)` ISO 폴백
- 3축 종합 평균 비교 — SRS Scenario 4 정합
- 직전 주 null → "—" 미노출 폴백
- `PredictionCard` Client Component + `prediction_clicked` trackEvent
- mock 예측 (currentAvg + 5) + **베타 라벨** (FR-C-011 통합 시 자동 미노출)
- vitest +13 (WoW delta 6 + PredictionCard 7)
- commit `8eddcb9` (+303 -25)

### 합산: vitest **356 pass** (+13, 회귀 0)

---

## 7. Sub-session H — FR-C-011 (Gemini 회귀 예측)

### 결정 — 단독 진행

- 사용자 결정: 병렬 권장 거절, 단독 진행
- 이유: G 의 mock 예측 (베타 라벨) 을 실 예측으로 대체 — user-visible improvement 의 핵심

### 구현

- **`lib/schemas/prediction.ts`** (NEW): Zod (PredictionInput/Output + GeminiOutput refine: `lower ≤ predicted ≤ upper`)
- **`app/actions/prediction.ts`** (NEW): `predictNextScore()` Server Action
  - 4주 history 수집 (aggregateWeeklyScores loop, 일부 0건이면 null)
  - Gemini generateJson (system: CON-04 의료 어휘 금지 명시)
  - SEC-004 RateLimiter 자동 적용 (userId 전달)
  - in-memory cache (24h TTL, envPrefix 격리, key: `env:userId:year-W:freq`)
  - RateLimitedError graceful: 직전 캐시 있으면 stale 반환 (`staleFromRateLimit=true`)
  - missionFrequency: low/normal/high 시뮬 옵션 → 별도 캐시 키
- **`events.ts`**: prediction_calculated + prediction_simulation_changed 추가
- **`PredictionCard` 재설계**:
  - `initialPrediction: PredictionResult | null` + `userId`
  - 4주 미만 → empty 분기 ("4주 누적 후" 카피)
  - staleFromRateLimit → "잠시 후 다시" 배지
  - 슬라이더 (radiogroup low/normal/high) — `useTransition` 으로 재호출
- **`reports/page.tsx`**: mock 예측 (currentAvg+5) 제거, predictNextScore 호출 (try/catch graceful)

### 결과

- AC 6/6 충족 (S6 lower ≤ predicted ≤ upper 는 Zod refine() 강제)
- vitest +11 (prediction action 8 + PredictionCard 재작성 8 net +3)
- 총 **367 pass**, 회귀 0
- commit `a8cb2f5` (+655 -91)

### 별도 task

- Upstash Redis 캐시 (AGENTS.md §3 "Redis 스택 외" 정책 변경 후)
- FR-C-010 Cron 통합 (weekly_reports.predictedNextScore 사전 저장)
- FR-Q-012 페이지 시뮬 결과 시각화 확장

---

## 8. Sub-session I — TEST-014 (HITL 9 시나리오 통합)

### 발견 매핑

- 기존 인프라 (lib/hitl.ts 3 함수 + cron + 7건 unit/integration test) 광범위 구현
- 누락 = sc6 abuse defense + sc9 expert count + 9 시나리오 통합 test

### lib/hitl.ts 확장

- `enqueueForReview` 패턴 전환: `upsert` → `findUnique + create/update` (abuse 검사 경로 분리)
- REQ-FUNC-034 어뷰징 방어: 동일 userId 월 3건 dismissed 시 4번째는 `status=dismissed` + `completedAt` 즉시
- `countDismissedThisMonth(userId, now)` — 캘린더 월 경계
- `countReviewsToday(expertId, now)` — UTC 일 경계
- `EXPERT_DAILY_THRESHOLD = 50` / `ABUSE_MONTHLY_THRESHOLD = 3` 상수 export

### cron/hitl-monitor 확장

- `findOverloadedExperts(now)` 집계 — 오늘 completedAt 별 expertId 그룹
- threshold 초과 시 admin Slack alert per expert (R4 보호: expertId 만, 자녀 식별 정보 0)
- 응답 JSON 에 `overloadedExpertCount` 필드 추가

### 9 시나리오 통합 test (`__tests__/integration/hitl-lifecycle.test.ts`)

| sc | 내용 | 검증 |
|---|---|---|
| 1 | Confidence < 70 → INSERT pending + slaDueAt = +48h | ✅ |
| 2+3 | Cron 24h+ → escalated + Slack 1건 | ✅ |
| 4 | Studio UPDATE → evaluationResult.hitlReviewed (D4 응용측 일관성) | ✅ |
| 5 | D4: Resend 미사용, Slack 대체 통보 | ✅ (deferred 명시) |
| 6 | 어뷰징 방어: 월 3건 dismissed → 4번째 auto dismissed | ✅ |
| 7 | groundTruthScore JSON 누적 (P2 model_retraining) | ✅ |
| 8 | 멱등성: 신규 escalated 0건 → Slack 0건 | ✅ |
| 9 | expert 1일 51건 → admin Slack alert + R4 보호 | ✅ |
| 격리 | 실 fetch mock 외 외부 호출 0 | ✅ |

### 기존 test 갱신 (패턴 이동 대응)

- `lib/hitl.test.ts`: upsert mock → findUnique+create+count 패턴 (3 case, 신규 abuse 케이스 포함)
- `hitl-flow.test.ts`: sc4~6 mock 패턴 + 시나리오 5 카피 동기화 (회귀 0)

### 결과

- AC 7/7 충족
- vitest +16 (총 **383 pass**, 회귀 0)
- commit `0dca38f` (+521 -42)

### 별도 task

- Resend 이메일 정식 통합 (D4 단순화 정책 종료 시)
- model_retraining_data 별도 테이블 (P2)
- PostgreSQL 트리거 실 DB 검증 (Supabase staging)

---

## 9. 본 sub-session 최종 상태

### 9.1 정량 누적

| 항목 | 시작 → 끝 | 증감 |
|---|---|---|
| commit | 8 → 23 누적 (오늘 단일 세션) | sub-session B~I = 9 신규 |
| push | 7 → 16 (사용자 승인) | 9 신규 |
| vitest | 301 → **383 pass** | +82 / 회귀 0 |
| vitest 파일 | 33 → 38 | +5 |
| Lint / tsc | 매 commit 0 / 0 | — |

### 9.2 Issue 종결 가능 (sub-session A + B~I 합산, 11건)

| Issue | 상태 |
|---|---|
| #74 SEC-004 | ✅ AC 6/6 |
| #26 FR-C-003 | ✅ AC 3/4 + 1 부하 별도 |
| #27 FR-C-004 | ✅ AC 3/4 + 1 외부 GUI 별도 |
| #16 DB-004 | ✅ AC 5/5 |
| #46 FR-Q-005 | ✅ AC 4/4 (Gemini 실 예측까지) |
| #47 FR-Q-006 | ✅ AC 5/5 |
| #67 MON-004 | ✅ Health + Status + Postmortem |
| #68 OPS-001 | ✅ AC 6/6 docs |
| #103 SP3_2A-3 | ✅ Option A (B/C 별도) |
| #34 FR-C-011 | ✅ AC 6/6 |
| #88 TEST-014 | ✅ AC 7/7 |
| **#44 FR-Q-003** (전날) | ✅ AC 4/6 |

### 9.3 병렬 패턴 누적 성과 (3회차)

| 회차 | task | 사용자 입력 | 회귀 | 통합 결과 |
|---|---|---|---|---|
| 1 (E) | DB-004 + SP3_2A-3 | 1회 검토/승인 | 0 | +18 tests |
| 2 (F) | OPS-001 + Status Page | 1회 검토/승인 | 0 | +7 tests, README auto-merge |
| 3 (G) | README CON-04 (직접) + FR-Q-005 잔여 (Agent) | 1회 검토/승인 | 0 | +13 tests |

→ **3 회차 모두 충돌 0, 회귀 0. 병렬 패턴 안정 정착.**

**활용 가능 조건** (학습):
1. 파일 set disjoint (events.ts / package.json 같은 공유 파일 수정 시 순차 안전)
2. 각 Agent isolated worktree
3. 통합 시 fast-forward / cherry-pick + 합산 lint/tsc/vitest

### 9.4 다음 sub-session 진입점

| 우선순위 | 작업 | 본 PC 가능 |
|---|---|---|
| 🟡 P1 | **#103 Option B** autocorrelation/YIN 알고리즘 도입 | ✅ (큰 재작성) |
| 🟡 P2 | FR-Q-009~014 / FR-C-015~018 / TEST-011~013 등 P2 issue | ✅ (매우 큼) |
| 🟢 외부 | UptimeRobot + Supabase Storage RLS + Vercel Cron 슬롯 정리 | 사용자 GUI |
| 🟡 정리 | FR-C-011 의 in-memory cache → Upstash Redis 이전 | AGENTS.md §3 정책 변경 후 |
| 🟡 비기술 | 사용자 모집 첫 채널 결정 + 첫 100 MAU | — |

### 9.5 사용자 약속 (재확인)

- 프로젝트 완료 전 analyzer 파라미터 + acousticScore 산식 재점검 (5/19 §20 + §24.4)
- 컴플라이언스 4건 (PIPA / 의료기기법 / STT 미국 전송 / Gemini PII) 별도 트랙 (Grill follow-up 중단 결정)

### 9.6 본 sub-session 운영 메모

- **AskUserQuestion** — 사용 0회 (작업 분기 명확, 추천 → 사용자 결정 패턴)
- **Agent 병렬 launch** — 3회차 모두 isolated worktree 로 성공. 사용자 부담 1회/회차로 효율 입증.
- **회귀 방지 자동화** — 매 commit 마다 lint/tsc/vitest 전체 실행. 단 한 번도 회귀 0 유지.
- **D4 단순화 정신** — TEST-014 의 Resend / model_retraining 등 큰 의존성 도입 회피, 응용측 일관성 + 정책 documentation 으로 대체.
- **메모리 규칙 준수** — push 마다 사용자 명시 승인. 대화기록 단일 일자 파일 (5/20 + 5/21 분리).

---

**— End of 2026-05-21 sub-session B~I (본 PC, P0 100% + P1 7건 + 병렬 3회차) —**

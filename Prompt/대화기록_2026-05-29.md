# 대화기록 — 2026-05-29

> 저장 위치: `Prompt/대화기록_2026-05-29.md`
> 운영 규칙: 직전 저장(`대화기록_2026-05-27.md` §22, `d8c2ae2` / 05-27 20:19)
> 이후의 미기록 작업을 back-fill 한 문서.
> 본 문서 핵심: **(A) 미션(missions) 콘텐츠/회귀 sub-session 인계 기록 + (B) 본 PC ↔ GitHub sync 확인 + (C) 다음 작업 정렬**

> ⚠️ **재구성 고지**: §1~§7 의 미션 sub-session 은 **다른 세션(Claude Opus 4.7)** 이
> 수행한 작업이며, 본 세션(Opus 4.8)이 **git 커밋 메시지 + diff stat 기반으로 재구성**한
> 기술 요약이다. 원본 대화 transcript 가 아니므로 커밋에 남지 않은 중간 판단/시도는 누락될 수 있다.

---

## 0. 세션 작업 요약

### 정량 결과 — 미션 sub-session (05-27 20:42 ~ 05-28 20:47)

| 항목 | 값 |
|---|---|
| **신규 commits** | 10 (`88286e7` ~ `2957204`) |
| **수행 모델** | Claude Opus 4.7 (1M context) — 커밋 Co-Authored-By |
| **성격** | prod 마이크 검증 후속 — 미션 시스템 회귀 fix + 난이도 1/2/3 콘텐츠 구축 + 발화 감지 micro-feedback |
| **vitest** | 2628 → **2667 PASS** (+39, 회귀 0) |
| **신규 컴포넌트** | 4 (MissionWordRepeat / MissionWordFill / MissionSentenceBuild / useVoiceActivity) |
| **신규 라우트** | `/missions/[missionId]/play` (난이도별 콘텐츠 inject) |
| **신규 데이터** | `lib/mocks/mission-content.ts` — 5 자모 × 3 난이도 = 15 sets |
| **prod build** | ✅ `next build` 통과 (21/21 static pages) |
| **CON-04 금칙어** | 0건 유지 (치료/진단/장애 등) |

### 본 세션(05-29) 메타 작업

| 항목 | 값 |
|---|---|
| **sync 확인** | 본 PC main = `origin/main` = `2957204` (ahead 0 / behind 0, clean) |
| **인계 갭 발견** | 미션 sub-session 10 commits 가 대화기록 미작성 상태였음 → 본 문서로 해소 |
| **다음 작업 정렬** | ① 본 기록 작성(현재) → ② VoiceRecordingForm 단위 테스트 |

### sub-session 위치

- 직전 기록: `대화기록_2026-05-27.md` §22 (본 PC F11 마이크 검증, `7ca9b9e` 종료)
- §22 종료 직후 동일 세션 흐름에서 **미션 시스템 prod 검증**으로 전환 → 회귀 다수 발견 → 본 sub-session
- 작업이 05-27 저녁부터 05-28 저녁까지 이어졌으나 대화기록 미작성 → 본 문서(05-29)에서 back-fill

---

## 1. 배경 — 미션 시스템 prod 검증

§22(F11 마이크 검증)에 이어 prod 사이트의 **미션(/missions) 흐름**을 마이크 가능 PC 에서
검증. 다른 PC 의 E2E/단위 테스트로는 못 잡은 실사용 회귀가 연쇄적으로 드러남.

핵심 발견 흐름:
- 미션 카드 "시작" → 전부 동일 음소의 진단 UI 로 fallback (미션 시스템 사실상 무용)
- 난이도 1/2/3 콘텐츠 컴포넌트 자체가 부재
- 미션 즉시 완료 → KPI inflate 가드 부재
- 한글 자음 missionId URL 진입 시 404
- 발화 감지 피드백 부재 ("수행만 하면 끝" 인상)

→ "회귀 fix + 콘텐츠 구축 + micro-feedback" 의 10-commit sub-session 으로 전개.

---

## 2. 회귀 fix 1차 — KPI 가드 · 음소 정합 · query 라우팅

### 2.1 `88286e7` — MIN 30s 완료 가드 + MirrorButton 통합 (★ W-AUR KPI 보호)

**회귀 3 (critical)**: 미션 시작 즉시 "완료" → `mission_completed` 발송 →
W-AUR(Weekly Active User Rate ≥ 60%) KPI inflate.
- 원인: `MissionRunner.finish()` 의 `elapsedSec` 검증 0건.
- Fix: `MIN_MISSION_DURATION_SEC=30` 상수. `manual_done` + 30초 미만 시
  warning 노출 + `mission_completed` 발송 차단 + phase 유지(running).
- `timer_ended`(자동) / `skipped`(명시)는 통과 — funnel 분석 분리.

**회귀 2 (UX)**: MissionRunner 에 MirrorButton 미통합 → 수동 mirror trigger 부재.
- Fix: running phase 에 `<MirrorButton>` 추가. 자동(90s 침묵) 활성 중에는 중복 회피로 hide.

### 2.2 `6a9941f` — dailyMissionFixtures 에 ㄹ 음소 추가 (4→5 자모, 12→15 카드)

- 증상: "다른 미션 둘러보기"에 10 카드만 (5 자모 × 3 난이도 = 15 기대).
- 원인: `lib/mocks/missions.ts` 의 `PHONEMES` 가 4 자모(ㅅㅈㄱㄴ)만 — ㄹ 누락. 진단(5 자모) ↔ 미션(4 자모) 정합 위반.
- Fix: `["ㄱ","ㄴ","ㅅ","ㅈ","ㄹ"]` — 한국어 음운론 위계(파열음→비음→마찰음→파찰음→유음, seed.ts 일치).
- 메모: DB-006 실 MissionCard(25 cards) 는 page 가 미사용(mock 의존). DB 연결 교체는 별도 sub-session 가치.

### 2.3 `e6259c4` — `?phoneme=` query 받기 + 카드 filter 제거 (회귀 2건)

**회귀 2 (★ critical — 모든 카드 무용지물)**: 어느 카드를 눌러도 같은 음소(`preferredPhonemes[0]`)로 이동.
- 원인: `/diagnose/page.tsx` 가 `searchParams` 무시.
- Fix: `searchParams.phoneme` valid(5 자모 화이트리스트) 시 우선 사용, fallback = user prefill. Next.js 16 async searchParams(Promise) 패턴 적용.

**회귀 1 (UX)**: filter 로 추천 카드 숨김 → "왜 ㄱ 따라하기가 안 보이지?" 혼란.
- Fix: filter 제거 + "★ 오늘의 추천" 라벨 + ring-emerald 시각 강조. 15개 모두 노출. 섹션명 "다른 미션" → "전체 미션 둘러보기".

> 30s 가드(2.1) 여파로 `mission-spl-flow.test.tsx` 2건 타이머 1k/10k → 30k 로 조정.

---

## 3. 콘텐츠 구축 — 난이도 1/2/3 컴포넌트

### 3.1 `41e6a17` — 난이도 2(빈칸) + 난이도 3(문장) 컴포넌트 신규 (FR-Q-003-CONTENT)

- 배경: 카드 "시작" → 전부 `/diagnose?phoneme=X` 진단 UI 로 fallback → 난이도별 기대 미충족.
- 신규:
  - `lib/mocks/mission-content.ts` — 5 자모 × 2 난이도 = 10 sets + Map lookup helper.
  - `components/missions/MissionWordFill.tsx` — 빈칸 → 정답 → 다음 progression.
  - `components/missions/MissionSentenceBuild.tsx` — 문장 + focusWord 강조 + 음절 가이드.
  - `app/(public)/missions/[missionId]/play/page.tsx` — missionId lookup → 난이도별 콘텐츠를 MissionRunner children 으로 inject, 미존재 시 `notFound`.
  - `MissionRunner.tsx` — optional `children` prop (running phase progressbar 아래 슬롯, 기존 호출자 무영향).
  - `/missions` 카드 "시작" href → `/missions/{id}/play`.
- 테스트: +20 (2629 → 2649). build 통과(라우트 등록 확인).

### 3.2 `7201816` — 난이도 1(단어 따라하기) 콘텐츠 + 추천 영역 통합 (FR-Q-003-CONTENT-V2)

- `components/missions/MissionWordRepeat.tsx` 신규 — 단어 카드 progression(text + 음절 분리 reading) + done UI.
- `mission-content.ts` — 난이도 1(5 자모) 추가, `MissionWordSimple` type. → 5 자모 × 3 난이도 = **15 sets**(10→15).
- `/missions` 추천 섹션 inline MissionRunner 도 난이도별 콘텐츠 inject → 카드 그리드와 정합화.
- `/play` 에 난이도 1 분기(MissionWordRepeat mount) 추가.
- 테스트: +7 (2649 → 2656). build 통과(21/21 static).

---

## 4. 한글 라우팅 회귀 — NFC normalize → ASCII slug

### 4.1 `bd113aa` — 한글 자음 NFC normalize 비교 (1차 시도)

- 증상: `/missions/mock-ㅅ-2/play` 등 한글 자음 missionId → 404 (`m.id === missionId` 실패).
- 원인 추정: fixture id ↔ URL path segment 의 unicode normalization(NFC vs NFD) 차이.
- Fix: 양쪽 `.normalize("NFC")` 후 비교.

### 4.2 `885e3c9` — fixture id 를 ASCII slug 로 변경 (근본 fix)

- NFC normalize(4.1)로도 404 지속 — Next.js 16 / Vercel 의 비 ASCII path segment 처리 깊은 이슈로 판단.
- Fix: `PHONEME_SLUG` 매핑(ㄱ→g, ㄴ→n, ㅅ→s, ㅈ→j, ㄹ→l) → fixture id ASCII 화(`mock-s-2`, `mock-l-3`). URL/lookup 모두 ASCII → 안전.
- 영향: testid 10건 변경(3 test file). bd113aa 의 NFC normalize 는 이제 영향 0 이지만 방어적으로 유지.

---

## 5. 미션 vs 진단 역할 분리 UX 카피 — `e1bb65e` (Option A)

- 배경: 미션 완료 시 평가/점수 없어 사용자가 수행 기준점 불명확.
- 변경:
  - `/missions` subtitle: "미션 = 연습, 진단 = 정확도 점수" 한 줄.
  - `/missions/[id]/play` 헤더에 sky-tone 안내 박스(`data-testid=practice-note`).
  - MissionRunner completed 카피 명확화 + "발음 연습으로 가기 →".
- 의도: W-AUR engagement loop(미션) ↔ actual scoring(/diagnose) 역할 분리 명시.

---

## 6. 발화 감지 micro-feedback — `useVoiceActivity`

### 6.1 `69471dc` — 발화 감지 micro-feedback 신규 (Option B / FR-Q-003-CONTENT-V3)

- `lib/audio/useVoiceActivity.ts` 신규 — `useSplMeter` 의 `currentDb` 를 input 으로 받아 별도 AnalyserNode 없이 speech state 추적(single source of truth).
  - 임계 40dB / minDuration 150ms / silenceMs 400ms(setTimeout 기반 silence 전환).
  - 반환: `isSpeaking`, `speechCount`, `lastSpeechAt`.
- MissionRunner 통합:
  - 발화 감지 시 `intervention.reportSpeech()` → 60s/90s 침묵 카운터 reset.
  - running phase 에 "● 듣고 있어요" indicator(`data-testid=voice-activity-indicator`).
  - **CON-04 유지**: 점수/평가 카피 X, 발화 boolean 시각화만.
- 의도: "수행만 하면 끝" 인상 완화 + "앱이 듣고 있다"(user feels heard).
- 테스트: +10 (useVoiceActivity 8 + MissionRunner indicator 2) → 2666.

### 6.2 `2957204` — 정적 임계 → baseline + dynamic threshold (prod 회귀 fix)

- 증상: indicator badge 가 항상 켜짐 — 발화 안 해도 "● 듣고 있어요". 디바이스 baseline noise 가 정적 임계 40dB 이상이면 상시 above-threshold.
- 원인: `useSplMeter` SPL 추정값(offset 100)이 디바이스/환경 따라 baseline 30~50dB 편차. 정적 임계는 false trigger 면역 불가.
- Fix:
  - baseline 측정 phase — 시작 직후 `baselineSamples`(default 10, 100ms × 10 = 1초) 동안 `currentDb` 평균을 `baselineDb` 로 측정.
  - speech 임계 = `baselineDb + baselineOffsetDb`(default 10).
  - baseline 측정 중 `isSpeaking=false` 고정(false trigger 방지).
  - `enabled=false` 전환 시 baseline 리셋 → 재활성화 시 재측정.
  - 반환값에 `baselineDb` 추가(디버그/조정용).
- 테스트: 8 → 9건(baseline phase 시나리오). → **2667/2667 PASS**.

---

## 7. 미션 sub-session 다음 진입점 (커밋에 남은 후속 메모)

| 우선순위 | 작업 | 출처 커밋 |
|---|---|---|
| 🟡 1 | `/missions` page 가 DB-006 실 MissionCard(25 cards) 사용하도록 교체 — fixture 의존 제거, 진단 결과 기반 추천 정확도 향상 | `6a9941f` 후속 |
| 🟡 2 | 회귀 5 — mirror overlay SVG 위치 + 음운학적 매핑 정확도(`phonemeToOverlay` 단순 분기 개선) | `e6259c4` 후속 |
| 🟡 3 | 카메라 mirror mode (FR-Q-014) prod 검증 — 마이크/카메라 PC 만 가능 | §22.8 #6 |

---

## 8. 본 세션(05-29) — 인계 갭 발견 + sync 확인 + 본 기록 작성

### 8.1 사용자 요청

```
"현재 이 프로젝트에서 다음 작업은 무엇인지 전체 파일을 확인해서 알려줘"
→ "깃허브 저장소와도 동기화 확인했어?"
→ "① 미션 세션 대화기록 작성 → ② VoiceRecordingForm 단위 테스트"
```

### 8.2 발견 — 문서 ↔ 코드 갭

- 최신 대화기록(`대화기록_2026-05-27.md` §22)은 `7ca9b9e`(F11)에서 종료.
- 그러나 git HEAD 는 `2957204` — 그 사이 **미션 sub-session 10 commits 가 대화기록 미작성** 상태.
- 즉 코드 sync 문제가 아니라 **문서화 누락** → 본 문서로 해소.

### 8.3 sync 확인 결과

- `git fetch origin` 후 `main = origin/main = 2957204` (ahead 0 / behind 0).
- 작업 트리 clean. §22.7-1 의 "다른 PC behind 누적" 패턴 이번엔 없음.

### 8.4 다음 작업

| 순번 | 작업 | 상태 |
|---|---|---|
| ① | 미션 sub-session 대화기록 작성(본 문서) | ✅ 완료 |
| ② | **VoiceRecordingForm 단위 테스트 작성** — §22.8 #3, 본 PC 마이크 검증 3 fix(closure stale `durationMs`, 0초 가드, error sticky) 회귀 가드 | ✅ 완료 |

### 8.5 ② VoiceRecordingForm 단위 테스트 결과

- 신규 파일: `__tests__/components/voice-clone/VoiceRecordingForm.test.tsx` (10 tests).
- 환경: happy-dom + MediaRecorder / getUserMedia / FileReader / scrollIntoView mock + fake timers(`Date.now()` 제어로 durationMs 검증).
- 커버리지:
  - 회귀1(durationMs closure stale): 35s 녹음 시 "30초 미만" 경고 **미노출** 가드 + 5s 시 경고 노출 보강.
  - 회귀2(1초 가드): 500ms 녹음 → recorded 진입 차단 + "너무 짧아요" + idle 유지 + track 정리.
  - 회귀3(error sticky): 라벨/동의 누락 에러 후 입력 시 자동 해제 + submit 실패 시 recorded 복원.
  - 부가: 누락 항목 scrollIntoView 호출 / inline 업로드 버튼 노출 조건 / 마이크 실패 / 성공 path.
- 검증: 단일 파일 10/10 PASS → 전체 **2667 → 2677 PASS**(회귀 0). tsc 신규 오류 0건(기존 `admin-reset-totp.test.ts` 4건만 잔존, 사전 합의 baseline).
- ⚠️ 미커밋 — 사용자 확인 후 commit/push (push 정책: 사용자 PowerShell 수동).

---

**— End of 대화기록 2026-05-29 — 미션 sub-session(Opus 4.7, 10 commits) back-fill + sync 확인 + VoiceRecordingForm 테스트 10건 작성(2677 PASS) —**

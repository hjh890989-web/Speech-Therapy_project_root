# 대화기록 — 2026-05-14

> 저장 위치: `Prompt/대화기록_2026-05-14.md`
> 운영 규칙: 2026-05-14 단일 세션 (오전 + 오후 + 야간 통합). `_N` 보조 파일 없음.
> 본 세션 핵심: **Sprint 1 마무리 → Sprint 2 §1-§4 (별 누적 1개 고착 완전 해결) → Sprint 3 §1 (3축 점수 분리) → Sprint 2 잔여 (FR-C-006~008 UI 통합) → API-010 §1 (Magic Link Auth + 익명 마이그레이션, 검증은 이메일 rate limit 으로 이연) → 디버그 endpoint 환경변수 게이트**

---

## 0. 세션 작업 요약

### 정량 결과 누적 (2026-05-14 1일)

| 항목 | 시작 | 종료 | 증분 |
|---|---|---|---|
| **Vitest** | 114/114 | **173/173** | +59 |
| **Test Files** | 15 | **22** | +7 |
| **진단 응답 시간** | 20s+ | **5~7s** | -70% |
| **신규 routes / workflows** | — | `/api/health` + 2 workflows + middleware (proxy) + `/api/debug/identity` + `/login` + `/auth/callback` | 7 |
| **신규 lib 모듈** | — | phonetic-similarity / linguistic-score / acoustic-score / anonymous-user (server-safe) / reward (재작성) / mission-recommender / supabase-server / supabase-client / hooks 2 | 10 |
| **신규 페이지** | — | `/login` (Magic Link) | 1 |
| **신규 DB 모델** | — | RewardLog (+ migration) | 1 |
| **메모리 파일** | 0 | **6** | +6 |
| **Sprint 진척** | Sprint 1 ≈95% | **Sprint 1 100% + Sprint 2 ≈80% + Sprint 3 §1** | — |
| **커밋 / 푸시** | — | **23 회** | 23 |

### 23 커밋 개요

| # | 커밋 | 작업 |
|---|---|---|
| 1 | `59c17b3` | FR-C-001 §A — 쿠션 텍스트 async 분리 (Suspense) |
| 2 | `d818c34` | FR-C-001 §B — Gemini lite + DB 병렬 + Slack fire-and-forget |
| 3 | `368ef70` | INFRA-004 + UX — Keep-warm cron + 진행 단계 메시지 |
| 4 | `ddb95ab` | fix(keep-warm) — curl -L 리다이렉트 추적 |
| 5 | `42d0ec7` | TEST-002 — HITL 통합 테스트 7 시나리오 |
| 6 | `71c45fe` | docs(prompt) — 대화기록 2026-05-14 (오전분) |
| 7 | `e0b3cfc` | INFRA-005 — GitHub Actions CI 워크플로 |
| 8 | `5fbd0df` | chore(ci) — workflow_dispatch 추가 |
| 9 | `32adc7b` | Sprint 2 §1 — anonymousId 추적 + reward_log 멱등성 |
| 10 | `46cf557` | Sprint 2 §2 — phonetic similarity (Gemini 텍스트 평가 폐기) |
| 11 | `858c2b3` | Sprint 2 §3 — cookie 권위 강화 (proxy.ts Set-Cookie + 자가 복구) |
| 12 | `9a4b1d0` | docs(prompt) — 대화기록 §1~§3 업데이트 |
| 13 | `b057718` | (이전 세션) 대화기록 2026-05-13 |
| 14 | `e9e8c61` | **Sprint 2 §4** — 별 누적 fix (localStorage 권위 + SW v3 + 디버그 endpoint) |
| 15 | `1ff4a2c` | **Sprint 2 §4 hotfix** — ANONYMOUS_USER_COOKIE server-safe 모듈 분리 |
| 16 | `95a2ffd` | **UX** — 결과 페이지 별 적립 배너를 /rewards 링크 카드로 |
| 17 | `ac24baa` | docs(prompt) — 대화기록 §4 추가 |
| 18 | `a3ab507` | **Sprint 3 §1** — 3축 점수 분리 재설계 (linguistic / acoustic 실 계산) |
| 19 | `d23fa69` | **Sprint 2 잔여** — FR-C-006 + FR-C-007 UI 통합 (침묵 격려 + 오프라인) |
| 20 | `4d58fdf` | **FR-C-008 UI 통합** — 미션 페이지 적응형 추천 (cookie + streak 분석) |
| 21 | `1849987` | **API-010 §1** — Supabase Magic Link Auth + 익명 → 인증 마이그레이션 |
| 22 | `1bf88a6` | **API-010 §1 hotfix** — signup 이메일의 / 루트 redirect 를 /auth/callback 으로 |
| 23 | `68f676f` | chore — /api/debug/identity 환경변수 게이트 |

---

## 1. 오전 — Sprint 1 마무리 (커밋 1~8)

### 1.1 진단 응답 시간 단축 (§A ~ §C, 70% 단축)

**문제 발견**: 모바일 스모크 테스트에서 진단 폼 제출 → 결과 페이지까지 **20초+** 소요.

**3단계 누적 최적화**:

| 단계 | 작업 | 응답 시간 |
|---|---|---|
| 초기 | — | 20s+ |
| **§A** (`59c17b3`) | 쿠션 텍스트 분리 (Suspense 후속 로드) | 20s+ (체감 미달) |
| **§B** (`d818c34`) | E (Gemini lite) + F (DB 병렬) + G (Slack fire-and-forget) | 10~15s |
| **§C** (`368ef70`, `ddb95ab`) | I (Keep-warm cron) + J (진행 단계 UX) | **5~7s** ✅ |

**§C 핵심 인프라**:
- `.github/workflows/keep-warm.yml` — 5분 cron (Vercel Hobby cron 슬롯 회피)
- `/api/health` 경량 endpoint + `/diagnose` 페이지 GET (Server Action 번들 워밍업)
- curl `-L` 플래그 (Vercel 엣지 308 리다이렉트 추적)

### 1.2 TEST-002 — HITL 통합 테스트 (`42d0ec7`)

`__tests__/integration/hitl-flow.test.ts` 신규 — FR-C-002 + API-005 + DB-009 결합:
- 7 시나리오 (confidence 65/75, Slack 호출, R4 검증, 멱등성, graceful, 즉시 이관)
- §C fire-and-forget 대응: `vi.waitFor` 로 microtask flush

### 1.3 GitHub Actions CI (`e0b3cfc`, `5fbd0df`)

`.github/workflows/ci.yml` — main push + PR 자동 회귀 검증 (tsc/lint/vitest, build 는 Vercel CI 책임).

### 1.4 메모리 이관 (커밋 미포함, ~/.claude/projects/.../memory/)

6 파일 생성:
- `MEMORY.md` (인덱스)
- `user_profile.md` (1인 바이브 코더, 한국어)
- `project_overview.md` (Home Language Coaching Platform)
- `project_chat_log_protocol.md` (대화기록 저장 룰)
- `feedback_main_push_pattern.md` (push 승인은 커밋별 1회)
- `reference_vercel_hobby_workarounds.md` (Hobby cron / Server Action 워밍업 / curl -L 등)

---

## 2. 오후 — Sprint 2 진행

### 2.1 Sprint 2 §1 — anonymousId + reward_log (`32adc7b`)

**목적**: 본 세션 모바일 스모크의 미해결 B2 (별 누적 +1) 해소 + 보상 멱등성 다중 인스턴스 안전.

**산출물**:
- `lib/hooks/useAnonymousUserId.ts` — localStorage + cookie 영구 식별자 (useSyncExternalStore)
- `DiagnosisForm` 에서 anonymousUserId 를 `analyzeDiagnosis` 에 전달
- `prisma/schema.prisma` — `RewardLog` 모델 (`@@unique([userId, idempotencyKey])`)
- `prisma/migrations/20260514120000_add_reward_log/migration.sql` — 수동 적용 (Supabase Studio SQL Editor → Run and enable RLS)
- `lib/reward.ts` — `grantReward()` 를 process-local Set → DB unique 제약 기반으로 재작성
- `app/(public)/rewards/page.tsx` — cookie 기반 anonymousUserId 조회로 실 데이터 표시
- 단위 테스트 4 종 신규/업데이트 (Vitest 128 → 132)

### 2.2 Sprint 2 §2 — phonetic similarity (`46cf557`)

**문제 발견**: 모바일 진단 시 다른 발음을 해도 점수가 항상 92점 고정.

**근본 원인**: Web Speech API STT 가 부정확한 발음을 보정해 정상 텍스트로 반환 → Gemini 가 텍스트만 보고 평가하므로 차이를 감지 못함.

**사용자 통찰**: "부모가 의도한 단어를 타이핑하고 STT 결과와 비교하는 것이 맞다."

**구현**:
- `lib/phonetic-similarity.ts` — 한글 자모 분해 + 음운 가중 Levenshtein
- 음운 그룹: ㅅ↔ㅈ 0.5 비용, ㅅ↔ㄱ 1.0 비용 등
- DiagnosisForm — 음소 선택 후 예시 단어 3개를 **클릭 가능 버튼** 으로 (intendedWord)
- analyzeDiagnosis — Gemini 호출 완전 제거, 결정적 알고리즘으로 articulationScore 산출
- 결과 페이지 — 의도/실현 비교 카드 (일치 시 초록, 차이 시 노란색)
- HITL 게이트 재정의: articulationScore < 50 (이전 confidence < 70)
- 단위 테스트 14 신규 (Vitest 132 → 146)

**부수 효과**: analyzeDiagnosis 에서 Gemini 호출 0회 → 응답 추가 단축 가능 (모바일 검증 진행 못함)

### 2.3 Sprint 2 §3 — cookie 권위 강화 (`858c2b3`)

**문제 발견 (Supabase Studio SQL 4 회 조사 결과)**:

| 데이터 | 값 | 해석 |
|---|---|---|
| 모바일 cookie | (불명, 1-star 사용자) | ITP 가 클리어/대체 |
| 모바일 localStorage | `0662c594-...` | 정상 영속 |
| EvaluationResult 최신 3건 userId | 모두 `0662c594-...` | form 은 정상 전달 |
| RewardLog count (`0662c594`) | **4** | 백엔드 누적 정상 |
| RewardProgress (`0662c594`).cumulativeStars | **4** | 백엔드 누적 정상 |
| 모바일 `/rewards` 화면 | **별 1개** | **cookie 의 다른 userId 가리킴** |

= localStorage 와 cookie 가 비동기화. iOS Safari ITP 가 `document.cookie` 로 설정한 쿠키를 캡/클리어 했음.

**Fix**:
- `proxy.ts` (Next.js 16 middleware) — cookie 부재 시 **Set-Cookie 응답 헤더** 로 발급 (ITP full TTL 존중)
- `useAnonymousUserId` 재설계 — **cookie 우선** / localStorage 폴백 / 자가 복구
- `analyzeDiagnosis.resolveUserId()` — cookie 우선 → input.anonymousUserId 폴백
- 테스트 2 케이스 추가 (Vitest 146 → 148)

**커밋 후 결과**: 모바일 재테스트 → **별 여전히 1개**. Sprint 2 §3 의 변경 효과 확인 못함 (Vercel 배포 지연 또는 추가 원인 가능).

---

## 2.4 Sprint 2 §4 — 별 누적 1개 고착 해결 + UX 마무리

본 세션의 이연 작업 (§3 미해결) 을 동일 일자 후속 세션에서 해결. 3 커밋 진행.

### 2.4.1 `e9e8c61` — 권위 방향 뒤집기 + SW v3 + 디버그 endpoint

**Root cause 확정**: §3 의 "cookie 권위" 가 [proxy.ts](Speech-Therapy_App/proxy.ts) 의 cookie 자동 발급과 결합해, ITP 클리어 시마다 새 UUID 가 localStorage 의 영속 값을 덮어씀 → 진단마다 다른 userId 에 별이 분산 저장.

**Fix**:
- [`lib/hooks/useAnonymousUserId.ts`](Speech-Therapy_App/lib/hooks/useAnonymousUserId.ts) — **localStorage 권위** 로 우선순위 뒤집기. cookie 가 다르면 localStorage 값으로 덮어쓰는 자가 복구
- [`app/actions/diagnosis.ts`](Speech-Therapy_App/app/actions/diagnosis.ts) `resolveUserId()` — `input.anonymousUserId` 우선 (hook 과 동일 방향)
- [`public/sw.js`](Speech-Therapy_App/public/sw.js) — `CACHE_NAME` v2 → v3 (구 응답 캐시 무효화)
- [`app/api/debug/identity/route.ts`](Speech-Therapy_App/app/api/debug/identity/route.ts) 신규 — 모바일 디버그용 (cookie + RewardProgress + rewardLogCount 즉시 응답)
- 테스트: §3 의 "cookie 권위" 단언 뒤집기 + "초기 발급 케이스" 신규 (Vitest 148 → 149)

### 2.4.2 `1ff4a2c` hotfix — server/client 모듈 경계 분리

**증상 (배포 후 PC 검증)**: cookieUserId 정상 발급 + DB 에 별 1 row 정상 누적 + /api/debug/identity 정상. **그러나 `/rewards` RSC 만 EmptyState**.

**Root cause**: `ANONYMOUS_USER_COOKIE` 상수가 `"use client"` 모듈 ([useAnonymousUserId.ts](Speech-Therapy_App/lib/hooks/useAnonymousUserId.ts)) 에서 export 됨 → server component ([/rewards/page.tsx](Speech-Therapy_App/app/(public)/rewards/page.tsx)) 가 import 시 **client boundary 평가로 undefined 가 전달**됨 → `cookieStore.get(undefined)` → progress null → EmptyState.

대조 증거: `/api/debug/identity` 는 같은 상수를 **local const 로 하드코딩**해서 정상 동작 — 정확히 이 가설과 일치.

**Fix**: [`lib/anonymous-user.ts`](Speech-Therapy_App/lib/anonymous-user.ts) 신규 (directive 없는 순수 모듈). `ANONYMOUS_USER_COOKIE` + `COOKIE_MAX_AGE_SEC` 본 모듈에서 export. 5 파일 (/rewards, /api/debug/identity, diagnosis.ts, proxy.ts, useAnonymousUserId) 모두 본 모듈 참조 통일.

### 2.4.3 `95a2ffd` UX — 별 적립 배너를 `/rewards` 링크화

사용자 피드백: "별 +1 적립!" 알림 후 별 도감으로 가는 길이 없었음. [`RewardOnMount.tsx`](Speech-Therapy_App/app/(public)/diagnose/result/[sessionId]/RewardOnMount.tsx) 배너 자체를 `<Link href="/rewards">` 카드로 전환 + 호버 상태 + 화살표 인디케이터.

### 2.4.4 검증 결과 (실 PC, 시크릿 모드)

| 단계 | 결과 |
|---|---|
| `/api/debug/identity` 첫 호출 | `cookieUserId: null` + hint "새로고침 후 재호출" — proxy.ts 동작 정상 |
| 새로고침 | `cookieUserId: 8e2b4311-...` 발급 ✓ |
| `/diagnose` 1회 진단 | 결과 페이지 "오늘의 별 +1 적립!" 배너 + Local Storage `anonymousUserId` = cookie 일치 |
| `/api/debug/identity` 재호출 | `cumulativeStars: 1`, `rewardLogCount: 1` ✓ |
| 진단 2회 더 | `cumulativeStars: 3`, `rewardLogCount: 3` ✓ |
| `/rewards` 화면 | 별 **3**, 나무 0, AI 그림 0 ✓ |

**모든 layer 정확히 일치** — cookie userId / DB 누적 / 화면 표시 / 진단 횟수.

---

## 3. 미해결 → 해결됨 — 별 누적 1개 고착

### 3.1 이전 세션 상태 (이제 해결됨)

- ~~모바일 `/rewards` 화면 = 별 1개 고착~~ → **§4 + hotfix 로 완전 해결**
- DB 백엔드는 정상 (0662c594 user 에 cumulativeStars=4 누적)
- Cookie 와 localStorage 가 다른 userId 가리키는 것이 가설이었으나, §3 fix 후에도 변화 없음 → §4 에서 두 layer 의 실제 원인 (권위 방향 + 모듈 경계) 모두 잡음

### 3.2 잠재 원인 후보 (다음 세션 우선 검증)

| 가능성 | 검증 방법 |
|---|---|
| Vercel 배포가 §3 (`858c2b3`) 까지 안 됨 | Vercel 대시보드 → 최신 deployment 가 `858c2b3` Ready 인지 |
| PWA Service Worker 가 구 응답 캐싱 | SW 캐시 v3 강제 무효화 (`public/sw.js` CACHE_NAME 버전 업) |
| 모바일 cookie 가 여전히 다른 값 (proxy.ts 가 작동 안 함) | PC + 마이크 → DevTools → cookie 직접 확인 |
| `/rewards` 페이지 RSC 가 잘못된 cookie 읽음 | 코드 점검 + 디버그 endpoint 추가 |

### 3.3 다음 세션 우선 작업 (마이크 있는 PC 사용)

1. **PC 마이크 있는 환경**에서 시크릿 모드로 전체 플로우 검증:
   - `/diagnose` → 진단 1회 → `/rewards` 별 카운트 확인
   - 결과 페이지 + `/rewards` 페이지 모두 DevTools 로 cookie 값 확인
   - 같은 세션에서 진단 1회 더 → 별 카운트 +1 되는지
2. Vercel 대시보드에서 `858c2b3` 배포 상태 확인
3. 만약 여전히 문제 → SW 캐시 v3 강제 무효화 + 디버그 endpoint 신규 추가

### 3.4 사용자 영향 (인지 필요)

- 기존 0662c594 의 4 stars 는 orphan (인증 도입 전엔 회수 불가)
- API-010 (Auth) 도입 시 해결 가능

---

## 4. 푸시 권한 운영 패턴 변경

본 세션에서 권한 시스템이 명시: *"prior approval was for an earlier specific commit, not a standing authorization for subsequent pushes"*.

→ **"main 직접 푸시 승인" 은 단일 커밋에만 유효**. 매 커밋마다 명시적 재승인 필요.

→ 체인 분리 패턴: stage → commit → push 를 별도 명령으로 실행 (한 명령으로 묶으면 권한 시스템이 체인 전체를 차단).

---

## 5. 야간 — Sprint 3 §1 + Sprint 2 잔여 + API-010 §1 + 디버그 게이트 (커밋 18~23)

### 5.1 Sprint 3 §1 — 3축 점수 분리 재설계 (`a3ab507`)

**문제**: 결과 페이지의 "조음 / 언어 / 음향" 3축 카드가 모두 articulationScore 동값 → 정보 가치 0. 사용자 검증에서 즉시 노출.

**Fix**:
- [`lib/linguistic-score.ts`](Speech-Therapy_App/lib/linguistic-score.ts) — 음절 수 일치도 (어휘 단위 완성)
  - "사과" vs "수갑" → 100 (음절 끝까지) / "사과" vs "사" → 50 (음절 누락)
- [`lib/acoustic-score.ts`](Speech-Therapy_App/lib/acoustic-score.ts) — 자모 길이 합리성 (50%) + STT 한글 명료성 (50%)
  - 영문/기호 섞이면 명료성 페널티
- `countHangulSyllables` 헬퍼 `phonetic-similarity.ts` 로 공유
- [`app/actions/diagnosis.ts`](Speech-Therapy_App/app/actions/diagnosis.ts) — `linguisticScore = computeLinguisticScore(...)`, `acousticScore = computeAcousticScore(...)`

**임상 시그널**: "사과" vs "수갑" 케이스 → articulation 낮음 + linguistic/acoustic 90+ → "조음 능력만 부족" 명확한 신호. 주간 리포트 3축 추이도 의미 회복.

**검증**: 실 PC `/diagnose` → 결과 페이지 3 카드 숫자 다르게 표시 확인 ✓ (사용자 "점수 다름").

### 5.2 Sprint 2 잔여 — FR-C-006 + FR-C-007 UI 통합 (`d23fa69`)

**FR-C-006 침묵 감지**:
- [`DiagnosisForm.tsx`](Speech-Therapy_App/app/(public)/diagnose/DiagnosisForm.tsx) 에 `useSilenceDetection` 활성 (status=listening/retrying)
- `transcript` 변경 시 `reportSpeech()` → 카운터 리셋
- 60s 초과 시 "어렵죠? 부모님과 함께 한 번 더 해볼까요?" 격려 카피 (실패 어휘 0)
- intervention state 를 hook 의 직접 반환값으로 사용 (`react-hooks/set-state-in-effect` 회피)

**FR-C-007 오프라인 처리 (D5)**:
- `useNetworkAware` — 상단 오프라인 배너 + 발화/제출 버튼 disable
- `analyzeDiagnosis` 호출을 `runWithRetry` 로 감싸 1회 자동 재시도
- mount 후에만 배너 노출 (hydration mismatch 회피)

**FR-C-008 미통합 사유 (당시)**: 미션 페이지 통합은 MissionCard DB seed + 익명 사용자 SessionLog 누적 선행 필요로 판단했었으나 → 5.3 에서 직접 통합.

### 5.3 FR-C-008 UI 통합 — 적응형 추천 (`4d58fdf`)

**아이디어 전환**: SessionLog 가 이미 누적되고 있고 (별 3개 = 진단 3회) `dailyMissionFixtures` 12 카드도 in-memory 로 있으므로, `analyzeStreaks` + `decideRecommendation` 을 직접 호출해 추천.

- [`lib/mission-recommender.ts`](Speech-Therapy_App/lib/mission-recommender.ts) — fixture 매칭 (정확 → 같은 phoneme → 같은 difficulty) + 격려 카피
- [`missions/page.tsx`](Speech-Therapy_App/app/(public)/missions/page.tsx) — cookie 의 anonymousUserId 로 최근 10 SessionLog + EvaluationResult join
  - articulationScore ≥ 70 → success
  - 가장 자주 발화한 음소 + streak 분석 → 적응형 난이도 결정
  - `/missions` 가 dynamic 라우트로 전환됨
- 격려 카피 (REQ-FUNC-021 "은밀히, X표시 0회"):
  - `continue`: "오늘의 추천"
  - `level_down`: "조금 익숙한 발음부터 시작해볼까요?"
  - `level_up`: "더 멋진 발음에 도전해볼까요?"
  - `phoneme_switch`: "새로운 발음을 만나볼까요?"
- 신규/익명 미사용자 → mockContinue 폴백

**테스트 +10**: mission-recommender 정확/근사 매칭 + 격려 카피 + findMostFrequentPhoneme.

### 5.4 API-010 §1 — Magic Link Auth + 익명 마이그레이션 (`1849987` + `1bf88a6` hotfix)

**스코프 결정 (사용자와 합의)**: 풀스펙은 multi-session 규모 (OAuth + RBAC + 5+ 보호 라우트). `/dashboard` `/admin` 라우트가 아직 없어 본 세션은 **§1 = identity 영속성** (Magic Link + 익명 마이그레이션) 만 집중.

**산출물**:
- [`lib/supabase/server.ts`](Speech-Therapy_App/lib/supabase/server.ts) + [`lib/supabase/client.ts`](Speech-Therapy_App/lib/supabase/client.ts) — `@supabase/ssr` SSR + 브라우저 클라이언트
- [`app/(public)/login/page.tsx`](Speech-Therapy_App/app/(public)/login/page.tsx) + [`LoginForm.tsx`](Speech-Therapy_App/app/(public)/login/LoginForm.tsx) — 이메일 입력 → `signInWithOtp` Magic Link
- [`app/auth/callback/route.ts`](Speech-Therapy_App/app/auth/callback/route.ts) — `exchangeCodeForSession` + Prisma User upsert + **익명 → 인증 마이그레이션**
  - SessionLog / EvaluationResult / RewardLog: userId 갱신
  - RewardProgress: @unique 충돌 시 합산 + 익명 row 삭제
  - 익명 User row 정리 (FK 없으면)
- [`app/actions/auth.ts`](Speech-Therapy_App/app/actions/auth.ts) — `signOut`
- [`AuthHeader.tsx`](Speech-Therapy_App/app/(public)/AuthHeader.tsx) + [`(public)/layout.tsx`](Speech-Therapy_App/app/(public)/layout.tsx) — 모든 공개 페이지 상단 인증 상태
- `/rewards` + `/missions` + `diagnosis.ts` 의 `resolveUserId` 모두 **인증 사용자 우선** 정책 통일

**Hotfix (`1bf88a6`)**:
- Supabase signup confirmation 이메일은 `emailRedirectTo` 무시하고 Site URL 로 redirect → `/?code=...` 가 루트로 도착 → 404
- Fix: `proxy.ts` 가 `/?code=` 패턴을 `/auth/callback?code=` 로 server-side redirect. Magic Link + signup confirmation 둘 다 호환.

**검증 차단 (이연)**: 이메일 검증 시 두 가지 차단 발견 (5.6 참조). 코드는 완성, 사용자 검증은 미수행.

### 5.5 디버그 endpoint 환경변수 게이트 (`68f676f`)

- `/api/debug/identity` 가 `DEBUG_ENDPOINTS_ENABLED=true` 일 때만 응답, 미설정 시 404
- 별 누적 디버깅 끝났으니 운영 환경에선 비활성. 향후 디버깅 필요 시 Vercel 환경변수 일시 추가 → 재배포

### 5.6 API-010 §1 검증 — 외부 의존 차단 두 건

**차단 1: Vercel 도메인 오타**:
- Supabase Site URL + Redirect URLs 가 모두 `speech-therapy-`**`porject`**`-root.vercel.app` (typo)
- Vercel 의 canonical 도메인은 `speech-therapy-project-root.vercel.app` (정상). typo alias 는 일부 옛 배포에만 묶여 있어 새 배포 promote 시 끊김
- 증상: `DEPLOYMENT_NOT_FOUND` 가 루트까지 확장
- Fix: Supabase Site URL/Redirect URLs 를 정상 spelling 으로 갱신 ✓

**차단 2: Supabase free tier 이메일 rate limit**:
- 시간당 ~4통 hard limit (built-in SMTP)
- 같은 IP/프로젝트 기준 — 다른 이메일 주소로 시도해도 막힘
- 검증을 위해선 **~1시간 대기** 또는 **Custom SMTP (Resend, Sendgrid 등)** 설정 필요
- 본 세션 검증은 이연. 코드는 완성 상태로 다음 세션 또는 사용자 임의 시점에 검증

---

## 6. Sprint 1 + Sprint 2 + Sprint 3 진척 정리

### 완료된 P0/Sprint 1 (2026-05-14 100%)

- 진단 응답 5~7s
- DB-001~011 + API-001~011 + FR-Q-001~007 + FR-C-001~009 + INFRA-001~004 + SEC-001/002
- TEST 인프라 (Vitest 148 + Playwright + TEST-002 통합)
- INFRA-005 GitHub Actions CI

### Sprint 2 (≈80%, 거의 완료)

- §1 anonymousId + reward_log 멱등성 ✅
- §2 phonetic similarity (Gemini 텍스트 평가 폐기) ✅
- §3 cookie 권위 강화 ✅
- §4 별 누적 1개 고착 fix + hotfix + UX ✅ **(실 PC 검증 완료, 모든 layer 정합)**
- 잔여 FR-C-006 + FR-C-007 + FR-C-008 UI 통합 ✅
- API-010 §1 Magic Link Auth + 익명 마이그레이션 ✅ (코드 완성, 사용자 검증 이연)
- 테스트: 148 → **173** (+25)

### Sprint 3 (시작)

- §1 3축 점수 분리 재설계 ✅ (linguistic / acoustic 실 계산)
- 잔여: §2 임상 신뢰성 강화 / §3 Sprint 3 잔여 ...

### Sprint 2 / 3 잔여 작업

- API-010 §1 사용자 검증 (Custom SMTP 설정 또는 rate limit 자연 해제 후)
- API-010 §2: OAuth (Google), RBAC middleware, email 한글 템플릿
- Sprint 3 §2+: 음성 신호 직접 측정 (Web Audio API), 임상 신뢰성 강화, 데이터 누적 후 임계값 보정

---

## 7. 메모리 / 운영 룰 변경 사항 (본 세션 추가 학습)

### 메모리 파일 (이미 저장됨, `~/.claude/projects/.../memory/`)

1. **Vercel Hobby cron 제약** — 슬롯 1 + daily-only → GitHub Actions 우회
2. **Server Action 워밍업** — `/api/health` 만 ping 불충분, 페이지 번들 GET 필요
3. **curl `-L` 필수** — 엣지 308 응답이 함수 실행 차단
4. **푸시 승인은 커밋별 1회용** — standing authorization 아님

### 본 세션 추가 학습 (메모리 갱신 후보)

1. **iOS Safari ITP 와 document.cookie 7일 캡** — Server Set-Cookie 만 full TTL 존중
2. **Next.js 16 middleware 컨벤션** — `proxy.ts` (root) + `export function proxy(req)`
3. **Web Speech API STT 보정 한계** — 텍스트만 비교 시 발음 차이 손실 → 결정적 자모 비교 필요
4. **한글 자모 분해 패턴** — `ㅘ` 는 단일 중성 자모 (ㅗ+ㅏ 합자 아님)
5. **Prisma migration 수동 적용 방법** — Supabase Studio SQL Editor 권장 (Run and enable RLS)

### §4 후속 세션 추가 학습

6. **anonymous 식별자 권위 방향** — cookie 는 ITP 가 클리어 + proxy 자동 발급이 결합되면 stale → **localStorage 권위가 정답**. cookie 는 RSC 가 읽는 거울일 뿐
7. **server / client 모듈 경계** — `"use client"` 모듈에서 export 된 상수를 server component 가 import 하면 client boundary 평가로 undefined 가 될 수 있음. **공통 상수는 directive 없는 server-safe 모듈로 분리** 필수
8. **Vercel middleware Set-Cookie 동작** — proxy.ts 의 `response.cookies.set()` 은 **다음 요청** 부터 cookies() 에 반영. API 라우트가 같은 요청에서 cookie 못 봄 → 새로고침 1회 필요

### 야간 추가 학습 (§5 작업 발견)

9. **Vercel 도메인 alias 신뢰 불가** — 프로젝트명에 오타 (`porject`) 가 있으면 일부 alias 는 옛 배포에만 묶여 새 배포 promote 시 끊김. 외부 시스템 (Supabase 등) 의 redirect URL 은 **canonical 도메인** 만 사용
10. **Supabase signup confirmation 흐름** — `signInWithOtp` 첫 호출 (신규 사용자) 은 confirmation 이메일이고, `emailRedirectTo` 가 무시되어 Site URL 로 redirect 됨. **middleware 단에서 `/?code=` → `/auth/callback?code=` forward** 가 가장 호환성 높은 fix
11. **Supabase free tier 이메일 rate limit** — built-in SMTP 는 시간당 ~4통 hard limit, **IP/프로젝트 단위** (다른 이메일 주소로도 안 풀림). 검증 단계에서 Custom SMTP (Resend 무료 3000/월) 설정이 거의 필수
12. **REQ-FUNC-021 "은밀히, X표시 0회" 임상 카피** — level_down 시 "조금 익숙한 발음부터 시작해볼까요?" / level_up 시 "더 멋진 발음에 도전해볼까요?" 패턴이 사용자에게 실패 인지를 노출하지 않음
13. **`react-hooks/set-state-in-effect` ESLint 회피** — 외부 hook 의 반환 state 를 그대로 사용하면 로컬 mirror state 불필요 → 효과 안의 setState 제거 가능

---

## 8. 다음 세션 진입 시 권장 시작점

### 즉시 우선 작업 (이전 세션 / 본 세션 미해결)

1. **API-010 §1 사용자 검증** — 두 가지 외부 의존이 막혀 코드만 완성 상태:
   - Supabase 이메일 rate limit (시간당 4통) → Custom SMTP (Resend 무료) 설정 권장
   - 검증 단계: `/login` 이메일 발송 → 클릭 → `/auth/callback` 정상 도착 → `/rewards` 별 마이그레이션 확인
2. (선택) **`/api/debug/identity` 활성화** — 검증 디버깅 필요 시 Vercel `DEBUG_ENDPOINTS_ENABLED=true` 임시 추가

### 다음 작업 후보

- **API-010 §2**: OAuth Google (이메일 우회 검증 경로), RBAC middleware (보호 라우트 부재로 우선순위 낮음), email 한글 템플릿
- **Sprint 3 §2+**: 음성 신호 직접 측정 (Web Audio API), 임상 신뢰성 강화 (데이터 누적 후 임계값 보정)
- **운영 진입 준비**: Custom SMTP / Supabase email 한글화 / Google OAuth Client / Vercel domain 정규화 (typo 제거)

### 본 세션 핵심 코드 위치

- [`proxy.ts`](Speech-Therapy_App/proxy.ts) — cookie 발급 + signup confirmation redirect forward
- [`lib/anonymous-user.ts`](Speech-Therapy_App/lib/anonymous-user.ts) — server-safe 공용 상수 (§4 hotfix)
- [`lib/hooks/useAnonymousUserId.ts`](Speech-Therapy_App/lib/hooks/useAnonymousUserId.ts) — localStorage 권위 (§4)
- [`lib/phonetic-similarity.ts`](Speech-Therapy_App/lib/phonetic-similarity.ts) — 한글 자모 + Levenshtein + `countHangulSyllables` (공유 헬퍼)
- [`lib/linguistic-score.ts`](Speech-Therapy_App/lib/linguistic-score.ts) — 음절 일치도 (Sprint 3 §1)
- [`lib/acoustic-score.ts`](Speech-Therapy_App/lib/acoustic-score.ts) — 자모 길이 + 한글 명료성 (Sprint 3 §1)
- [`lib/mission-recommender.ts`](Speech-Therapy_App/lib/mission-recommender.ts) — fixture 매칭 + 격려 카피 (FR-C-008)
- [`lib/curriculum.ts`](Speech-Therapy_App/lib/curriculum.ts) — `analyzeStreaks` / `decideRecommendation`
- [`lib/supabase/server.ts`](Speech-Therapy_App/lib/supabase/server.ts) + [`lib/supabase/client.ts`](Speech-Therapy_App/lib/supabase/client.ts) — `@supabase/ssr` 클라이언트 (API-010)
- [`app/actions/diagnosis.ts`](Speech-Therapy_App/app/actions/diagnosis.ts) — `resolveUserId` 인증 사용자 우선
- [`app/actions/auth.ts`](Speech-Therapy_App/app/actions/auth.ts) — `signOut`
- [`app/auth/callback/route.ts`](Speech-Therapy_App/app/auth/callback/route.ts) — exchange + Prisma User upsert + **익명 데이터 마이그레이션**
- [`app/(public)/login/page.tsx`](Speech-Therapy_App/app/(public)/login/page.tsx) + [`LoginForm.tsx`](Speech-Therapy_App/app/(public)/login/LoginForm.tsx) — Magic Link
- [`app/(public)/AuthHeader.tsx`](Speech-Therapy_App/app/(public)/AuthHeader.tsx) + [`(public)/layout.tsx`](Speech-Therapy_App/app/(public)/layout.tsx) — 모든 공개 페이지 상단 인증 상태
- [`app/(public)/missions/page.tsx`](Speech-Therapy_App/app/(public)/missions/page.tsx) — FR-C-008 적응형 추천 (cookie + streak)
- [`app/(public)/diagnose/DiagnosisForm.tsx`](Speech-Therapy_App/app/(public)/diagnose/DiagnosisForm.tsx) — silence detection + offline-aware
- [`app/api/debug/identity/route.ts`](Speech-Therapy_App/app/api/debug/identity/route.ts) — 디버그 endpoint (`DEBUG_ENDPOINTS_ENABLED` 게이트)
- [`lib/reward.ts`](Speech-Therapy_App/lib/reward.ts) — RewardLog @@unique 멱등성
- [`prisma/schema.prisma`](Speech-Therapy_App/prisma/schema.prisma) — RewardLog 모델
